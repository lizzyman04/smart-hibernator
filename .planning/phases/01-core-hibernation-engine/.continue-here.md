---
context: phase
phase: 01-core-hibernation-engine
task: null
total_tasks: null
status: planning_in_progress
last_updated: 2026-04-28T20:20:35Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **MV3 SW alarm placement** — `ensureHibernateAlarm()` MUST be called at module top level in `src/background/index.ts` (OUTSIDE `onInstalled`), not only inside it. Already fixed in Plan 01, but executor must verify this is present before build. If missing, auto-hibernation silently stops after first SW restart.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Alarm inside onInstalled only | `ensureHibernateAlarm()` placed only inside `onInstalled` means alarm is never recreated after SW restarts without a reinstall event. RESEARCH.md Pitfall 1. | blocking | Plan 01 Task 3 acceptance criteria requires `grep -n "ensureHibernateAlarm" src/background/index.ts` shows a call BEFORE first `addListener` |
| Tailwind arbitrary values | Using `w-[Xpx]` or `h-[Ypx]` in popup violates MV3 `script-src 'self'` CSP | advisory | Plan 03 acceptance criteria: `grep -c "w-\[" src/popup/App.tsx` must return 0 |

<current_state>
Planning Phase 1 (Core Hibernation Engine). `/gsd-plan-phase 01` was run. 4 PLAN.md files exist. Plan checker ran and found 6 blockers. 4 have been fixed. 2-3 minor documentation-level fixes remain before the checker will pass and execution can begin.
</current_state>

<completed_work>

- Plan 01 (Wave 0: Project Scaffold) — created by gsd-planner agent, includes full isDiscardable() implementation + 12 unit tests
- Plan 02 (Wave 1: Background SW hibernation loop — handleAlarmTick implementation)
- Plan 03 (Wave 2: Popup UI — full React App.tsx)
- Plan 04 (Wave 3: Tests — handleManualHibernate unit tests + Playwright E2E)
- Plan checker run → 6 blockers found
- Fixed: ensureHibernateAlarm() moved to module top level in Plan 01 (Pitfall 1 fix)
- Fixed: Plan 04 must_haves updated — no more callListeners escape hatch
- Fixed: Plan 04 task 1 action replaced — uses direct handleManualHibernate() testing
- Fixed: Plan 04 task 1 acceptance_criteria updated — requires 3 tests passing, no skips
</completed_work>

<remaining_work>

**Remaining plan checker blockers (fix these before re-running checker):**

1. **Plan 04 task 2 (NFR-06 gate):** The E2E task 2 action still has a "bail-out" paragraph allowing the full E2E to be skipped. Remove it. Add `web-ext lint` as the automated NFR-06 gate:
   - Add to Plan 04 task 2 `<action>`: `npm install -D web-ext && npx web-ext lint --source-dir=dist`  
   - Add to Plan 04 task 2 `<acceptance_criteria>`: `npx web-ext lint --source-dir=dist` exits 0
   - Remove the paragraph starting "If E2E tests fail due to headless mode..."
   - Remove: "The manual verification documented in VALIDATION.md covers NFR-06. E2E tests are a bonus; the manual smoke test is the required gate."

2. **Plan 03 objective (UI-SPEC contradiction):** Add one sentence to the objective noting the resolved contradiction:
   - In `01-03-PLAN.md` objective, add: "Note: UI-SPEC §6 Component Spec states zero-state shows '0' in text-zinc-600; the UI-SPEC Copywriting Contract states 'No tabs hibernated'. This plan adopts the Copywriting Contract version as the canonical specification. The Component Spec §6 zero-state row is superseded by the Copywriting Contract."

3. **RESEARCH.md open questions:** Change `## Open Questions` to `## Open Questions (RESOLVED)` and add a resolution note under each of the 3 questions:
   - Q1 (CRXJS + Vite 8): Resolved — Plan 01 Task 1 adds `--legacy-peer-deps` fallback and Vite 6 pin fallback
   - Q2 (autoDiscardable): Resolved — Do NOT add as guard; extension-initiated discard overrides browser setting per API intent
   - Q3 (React 19 HMR): Resolved — Popup HMR is non-critical; functional correctness unaffected; workaround is manual extension reload

**After all 3 fixes:** Run `/gsd-plan-phase 01` again — it will detect existing plans and offer to re-verify. The checker should pass this time.
</remaining_work>

<decisions_made>

- **4-plan wave structure:** Plan 01 (Wave 0 scaffold) → Plan 02 (Wave 1 hibernation loop) → Plan 03 (Wave 2 popup UI) → Plan 04 (Wave 3 tests)
- **FR-04 test approach:** Direct `handleManualHibernate()` testing instead of vitest-chrome `callListeners` — reliable, no API compatibility risk
- **Zero-state count display:** "No tabs hibernated" prose (Copywriting Contract) wins over "0" in text-zinc-600 (Component Spec §6)
- **NFR-06 gate:** `web-ext lint` provides automated MV3 manifest validation at build time
- **Alarm placement:** `ensureHibernateAlarm()` at module top level + inside onInstalled (defense-in-depth against Pitfall 1)
</decisions_made>

<blockers>
- 2-3 documentation-level fixes needed before plan checker passes (see remaining_work above)
- No architectural or structural blockers — plans are substantively correct
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-core-hibernation-engine/01-CONTEXT.md` — locked decisions D-01 through D-08 (must not be violated)
2. `.planning/phases/01-core-hibernation-engine/01-RESEARCH.md` — architecture patterns, anti-patterns, open questions
3. `.planning/phases/01-core-hibernation-engine/01-01-PLAN.md` through `01-04-PLAN.md` — current plan state
4. `.planning/HANDOFF.json` — machine-readable task list with what's done vs. remaining

## Infrastructure State
- No running servers or watchers
- No npm packages installed yet (greenfield project)
- Git: all plan files uncommitted (WIP)

<context>
Planning /gsd-plan-phase 01 for Smart Hibernator. The planner agent produced Plan 01 in a prior session. Plans 02-04 were written manually in this session. The plan checker found 6 blockers; 4 were fixed during this session. The 2-3 remaining fixes are small (one paragraph removal, one note addition, one section heading update) — they do not change the architectural decisions or task actions in any meaningful way. The plans are ready for execution once checker passes.
</context>

<next_action>
Start with: Fix the 3 remaining plan checker blockers listed in `<remaining_work>` (Plan 04 task 2 NFR-06 gate, Plan 03 objective note, RESEARCH.md open questions heading). Then run `/gsd-plan-phase 01` to trigger a fresh plan checker run. Once checker passes, run `/gsd-execute-phase 01`.
</next_action>
