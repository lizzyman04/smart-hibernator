---
phase: 03-ai-intelligence
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/background/ai-learning.ts
  - src/background/ai-learning.test.ts
  - src/background/hibernation.ts
  - src/background/hibernation.test.ts
  - src/background/index.ts
  - src/background/index.test.ts
autonomous: true
requirements:
  - FR-06
  - FR-07
  - NFR-04

must_haves:
  truths:
    - "ai-learning.ts exports recordKeepAlive, recordTabActivation, closeTabVisit, recordWakeMisclassification"
    - "recordKeepAlive increments biasOffset by 0.2 clamped to AI_BIAS_MAX and persists via putDomainBias"
    - "recordWakeMisclassification only applies a bias delta if Date.now() - existing.updatedAt is within AI_WAKE_SIGNAL_WINDOW_MS"
    - "closeTabVisit appends a TabHistoryRecord to the tab-history IndexedDB store on tab deactivation or removal"
    - "isDiscardable() returns false when classification.label === 'Vital' regardless of inactivity"
    - "isDiscardable() requires elapsed >= timeoutMs * 1.5 when classification.label === 'Semi-Active'"
    - "isDiscardable() requires elapsed >= timeoutMs * 0.5 when classification.label === 'Dead'"
    - "isDiscardable() falls back to base timeoutMs when classification is undefined OR confidence < AI_CONFIDENCE_THRESHOLD OR label is null"
    - "handleAlarmTick reads ai_classifications from storage in the existing atomic get call and triggers classifyBatch for candidate tabs"
    - "index.ts registers chrome.tabs.onActivated, chrome.tabs.onUpdated, chrome.tabs.onRemoved hooks that call recordTabActivation/closeTabVisit"
    - "index.ts handles KEEP_ALIVE message type by calling recordKeepAlive"
    - "index.ts calls ensureOffscreen() at module top level so offscreen document warms up on SW boot"
    - "onInstalled install handler writes ai_install_date and ai_classifications defaults; update handler backfills both keys"
    - "npm test exits 0 with new behavioral tests for ai-learning, isDiscardable AI integration, and SW wiring"
  artifacts:
    - path: "src/background/ai-learning.ts"
      provides: "Per-domain bias adjustment, behavioral event recording, visit-start/end tracking (module-level openVisits Map)"
      exports: ["recordKeepAlive", "recordTabActivation", "closeTabVisit", "recordWakeMisclassification"]
    - path: "src/background/hibernation.ts"
      provides: "Extended isDiscardable() accepting optional ClassificationResult; handleAlarmTick reads ai_classifications and triggers classifyBatch"
      contains: "ClassificationResult"
    - path: "src/background/index.ts"
      provides: "Top-level ensureOffscreen() call; behavioral event hooks; KEEP_ALIVE message handler; ai_install_date + ai_classifications storage defaults"
      contains: "KEEP_ALIVE"
  key_links:
    - from: "src/background/hibernation.ts isDiscardable"
      to: "ClassificationResult.label === 'Vital'"
      via: "AI integration block after structural guards, before base inactivity check"
      pattern: "Vital"
    - from: "src/background/hibernation.ts handleAlarmTick"
      to: "ai_classifications storage key"
      via: "chrome.storage.local.get(['...', 'ai_classifications'])"
      pattern: "ai_classifications"
    - from: "src/background/hibernation.ts handleAlarmTick"
      to: "classifyBatch from classifier.ts"
      via: "import + invocation before per-tab discard loop"
      pattern: "classifyBatch"
    - from: "src/background/index.ts top-level"
      to: "ensureOffscreen from classifier.ts"
      via: "import + synchronous call alongside ensureHibernateAlarm()"
      pattern: "ensureOffscreen"
    - from: "src/background/index.ts onMessage"
      to: "recordKeepAlive from ai-learning.ts"
      via: "KEEP_ALIVE message type branch"
      pattern: "KEEP_ALIVE"
    - from: "src/background/index.ts onActivated/onUpdated/onRemoved"
      to: "recordTabActivation/closeTabVisit from ai-learning.ts"
      via: "fire-and-forget call inside existing listener"
      pattern: "recordTabActivation"
---

<objective>
Wave 2 closes the loop: behavioral events flow into IndexedDB, the alarm tick produces classifications, and `isDiscardable()` applies the V/S/D timeout multipliers.

Deliverables:
1. Create `src/background/ai-learning.ts` — module-level `openVisits: Map<number, number>` (tabId → visitStart); functions for `recordKeepAlive(tabId, domain)` (D-09 explicit signal), `recordTabActivation(tabId, now)` (opens visit window), `closeTabVisit(tabId, hadFormActivity, url)` (appends TabHistoryRecord on deactivation/removal), `recordWakeMisclassification(tabId, domain)` (implicit signal — D-09; only fires within AI_WAKE_SIGNAL_WINDOW_MS).
2. Extend `src/background/hibernation.ts`:
   - Extend `isDiscardable()` signature with optional 7th param `classification?: ClassificationResult`. AI integration block per D-04/D-05/D-06/D-07: Vital → return false; Semi-Active → use 1.5× timeout; Dead → use 0.5× timeout; low confidence / null label / undefined classification → fall back to base timeoutMs.
   - Extend `handleAlarmTick()`: include `ai_classifications` in the existing atomic storage.get; identify candidate tabs (HTTP/HTTPS, not active, not pinned, not audible, status complete, not in protectedTabs/Domains); call `classifyBatch(candidates)` BEFORE the per-tab discard loop; pass per-tab classification into isDiscardable.
3. Extend `src/background/index.ts`:
   - Top-level `ensureOffscreen()` call alongside `ensureHibernateAlarm()` so the offscreen doc warms on every SW boot.
   - In `onInstalled` install branch, write `ai_install_date: Date.now()` and `ai_classifications: {}`; backfill both keys in the update branch.
   - Extend `chrome.tabs.onActivated`: keep existing storage write; ALSO close any previous open visit for the prior active tab and call `recordTabActivation(tabId, now)`. (Strategy: maintain a single "currently-active tabId per window" so we close the previous visit before opening the new one — simplest: in `recordTabActivation`, if openVisits has an entry for a different tabId, close it implicitly. But since we cannot look up url at close-time without storage, just append visit on `onUpdated` `status === 'complete'` OR on `onActivated` for the previously-active tab. The pragmatic approach for this plan: only START visits in `onActivated`; CLOSE visits in `onRemoved` AND when a new tab becomes active in the same window via `recordTabActivation` calling `closeTabVisit` on the prior-window-active tab.)
   - In `chrome.tabs.onRemoved`: call `closeTabVisit(tabId, hadFormActivity, lastKnownUrl)`. To know hadFormActivity, read `tab_meta[tabId].lastFormActivity` from storage before delete. lastKnownUrl is cached in `openVisits` parallel map or read from chrome.tabs.get before removal (this fails for already-removed tabs; fall back to a parallel `openUrls: Map<number, string>` in ai-learning.ts that is populated in `recordTabActivation`).
   - Add an `if (message.type === 'KEEP_ALIVE' ...)` branch to the existing `chrome.runtime.onMessage` listener — invokes `recordKeepAlive(message.tabId, message.domain).catch(() => {})`.
4. Expand `ai-learning.test.ts`, `hibernation.test.ts`, and `index.test.ts` with the new behavioral assertions.

Purpose: Wave 3 (UI) reads `ai_classifications` from storage and shows V/S/D pill. Wave 4+ is not in scope. This wave is the critical "AI affects hibernation" delivery.

Output: ai-learning.ts module, extended hibernation.ts + index.ts, full test coverage. `npm test` exits 0.
</objective>

<execution_context>
@/home/lizzyman04/.claude/get-shit-done/workflows/execute-plan.md
@/home/lizzyman04/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ai-intelligence/03-CONTEXT.md
@.planning/phases/03-ai-intelligence/03-RESEARCH.md
@.planning/phases/03-ai-intelligence/03-PATTERNS.md
@.planning/phases/03-ai-intelligence/03-VALIDATION.md
@.planning/phases/03-ai-intelligence/03-01-SUMMARY.md
@.planning/phases/03-ai-intelligence/03-02-SUMMARY.md

<interfaces>
After plan 03-02 ships, these exist:

From src/background/idb.ts (Wave 1):
```typescript
export async function appendTabHistory(record: TabHistoryRecord): Promise<void>
export async function getTabHistoryByDomain(domain: string, since: number): Promise<TabHistoryRecord[]>
export async function countTabHistory(): Promise<number>
export async function pruneTabHistory(cutoff: number): Promise<void>
export async function getDomainBias(domain: string): Promise<DomainBiasRecord | undefined>
export async function putDomainBias(record: DomainBiasRecord): Promise<void>
```

From src/background/classifier.ts (Wave 1):
```typescript
export function getDomainCategoryBoost(domain: string): number
export async function buildFeaturesForTab(tabId: number, url: string, meta: TabMeta): Promise<number[] | null>
export async function ensureOffscreen(): Promise<void>
export async function classifyBatch(candidateTabs: Array<{ tabId: number; url: string; meta: TabMeta }>): Promise<void>
```

From src/background/hibernation.ts (Phase 2 — current signature to be extended):
```typescript
export function isDiscardable(
  tab: chrome.tabs.Tab,
  meta: TabMeta | undefined,
  now: number,
  protectedTabs: number[],
  protectedDomains: string[],
  timeoutMs: number
): boolean
export async function handleAlarmTick(): Promise<void>
export async function handleManualHibernate(tabId: number): Promise<void>
```

From src/background/index.ts (Phase 2 — current listeners to be extended; never moved off top level):
- chrome.alarms.onAlarm → handleAlarmTick
- chrome.tabs.onActivated → updates tab_meta lastActiveAt
- chrome.tabs.onUpdated → captureAndStore on complete + active
- chrome.tabs.onRemoved → delete tab_meta + deleteThumbnail
- chrome.runtime.onMessage → FORM_ACTIVITY, MANUAL_HIBERNATE, CAPTURE_TAB
- chrome.contextMenus.onClicked, chrome.commands.onCommand
- onInstalled install/update branches populating storage defaults

From src/shared/types.ts (Wave 0):
- TabVitality, ClassificationResult, TabHistoryRecord, DomainBiasRecord, extended StorageSchema

From src/shared/constants.ts (Wave 0):
- AI_CONFIDENCE_THRESHOLD = 0.6
- AI_WAKE_SIGNAL_WINDOW_MS = 5 * 60 * 1000
- AI_BIAS_MAX = 1.0
- AI_LEARNING_DAYS = 14
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup (untrusted UI input) -> Service Worker KEEP_ALIVE | chrome.runtime.onMessage from popup is same-extension origin; still validate message.tabId is a number and message.domain is a string with reasonable length |
| Behavioral history -> Feature vector | Read by classifier; clamping happens in Wave 1 buildFeaturesForTab; Wave 2 writers must persist clamped biasOffset (clamp on write) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | recordKeepAlive biasOffset value (model-poisoning analog) | MEDIUM | mitigate | Clamp on write: `Math.min(AI_BIAS_MAX, (existing.biasOffset ?? 0) + 0.2)`; reject NaN inputs by treating undefined existing.biasOffset as 0 |
| T-03-12 | Spoofing | KEEP_ALIVE message from non-popup sender | LOW | mitigate | Validate `typeof message.tabId === 'number'` AND `typeof message.domain === 'string'` AND `message.domain.length < 256`; do not trust the sender frame; same-extension origin guaranteed by chrome.runtime |
| T-03-13 | Denial of Service | openVisits / openUrls Map memory growth | LOW | mitigate | Maps are keyed by tabId; entries deleted in closeTabVisit; chrome.tabs.onRemoved guarantees cleanup; bounded by open tab count |
| T-03-14 | Repudiation | Misclassification window allows out-of-window bias writes | LOW | mitigate | recordWakeMisclassification short-circuits when `Date.now() - existing.updatedAt > AI_WAKE_SIGNAL_WINDOW_MS`; no write happens outside window |
| T-03-15 | Information Disclosure | ai_install_date timestamp | LOW | accept | Install timestamp is per-device-only and never leaves chrome.storage.local (NFR-04 invariant preserved) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create src/background/ai-learning.ts with bias write + visit tracking; expand ai-learning.test.ts</name>
  <files>src/background/ai-learning.ts, src/background/ai-learning.test.ts</files>
  <read_first>
    - src/background/idb.ts (after Wave 1 — verify the new exports getDomainBias, putDomainBias, appendTabHistory, countTabHistory exist)
    - src/background/ai-learning.test.ts (Wave 0 stub from plan 03-01)
    - src/shared/types.ts (TabHistoryRecord, DomainBiasRecord)
    - src/shared/constants.ts (AI_BIAS_MAX, AI_WAKE_SIGNAL_WINDOW_MS)
    - .planning/phases/03-ai-intelligence/03-PATTERNS.md (ai-learning.ts section — exact function shapes)
    - .planning/phases/03-ai-intelligence/03-RESEARCH.md (D-09 learning trigger spec; Pitfall 5 cold start)
    - src/background/thumbnail.ts (analog — silent try/catch idiom)
  </read_first>
  <action>
    Per D-09 (implicit + explicit learning), D-10 (per-domain threshold update — no model retraining), T-03-11 (clamp on write), T-03-13 (Map cleanup), T-03-14 (window short-circuit).

    Create `src/background/ai-learning.ts` exporting exactly:

    1. Module-level state (replicate idb.ts singleton style; NEVER inside listeners):
       - `const openVisits = new Map<number, number>()` — tabId → visitStart unix ms
       - `const openUrls = new Map<number, string>()` — tabId → last-known url (snapshot at recordTabActivation; needed at closeTabVisit when chrome.tabs.get may fail for already-removed tab)

    2. `async function recordKeepAlive(tabId: number, domain: string): Promise<void>` per D-09 explicit signal + T-03-11:
       - Read existing bias via `getDomainBias(domain)`.
       - Build `next: DomainBiasRecord` with: `domain`, `biasOffset = Math.min(AI_BIAS_MAX, (existing?.biasOffset ?? 0) + 0.2)` (D-14 — strong positive Vital signal), `keepAliveCount = (existing?.keepAliveCount ?? 0) + 1`, `misclassificationCount = existing?.misclassificationCount ?? 0`, `updatedAt = Date.now()`.
       - Guard against NaN: if `Number.isNaN(existing?.biasOffset)`, treat as 0.
       - Persist via `putDomainBias(next)`.
       - Wrap entire body in try/catch; on error, silently continue.

    3. `function recordTabActivation(tabId: number, timestamp: number, url: string): void`:
       - If `openVisits.has(tabId)` (re-activation without closure), do nothing — keep the original visitStart so dwell accumulates correctly. Optionally update `openUrls.set(tabId, url)` so the close path uses the latest url.
       - Else: `openVisits.set(tabId, timestamp); openUrls.set(tabId, url)`.
       - Synchronous — does NOT await IDB; the close call writes the record.

    4. `async function closeTabVisit(tabId: number, hadFormActivity: boolean): Promise<void>`:
       - Pop visit start: `const visitStart = openVisits.get(tabId); if (!visitStart) return;` (already closed or never opened).
       - Pop url: `const url = openUrls.get(tabId) ?? ''`.
       - Delete map entries: `openVisits.delete(tabId); openUrls.delete(tabId);` (T-03-13 cleanup).
       - If `!url`, return silently.
       - Parse `domain = new URL(url).hostname` inside a try/catch — if invalid, return silently.
       - Build `record: TabHistoryRecord` with all fields: `domain`, `url`, `visitStart`, `visitEnd = Date.now()`, `dwellMs = visitEnd - visitStart`, `hadFormActivity`, `timestamp = visitStart`. Do NOT set `id` — it auto-increments.
       - Call `appendTabHistory(record)`. Wrap in outer try/catch — silent on error.

    5. `async function recordWakeMisclassification(domain: string): Promise<void>` per D-09 implicit + T-03-14:
       - Read existing bias via `getDomainBias(domain)`.
       - If existing exists AND `Date.now() - existing.updatedAt > AI_WAKE_SIGNAL_WINDOW_MS`, return without writing (outside short window = not a misclassification signal).
       - Note: if no existing bias record yet, we still want to record the first wake — treat as `updatedAt = 0` (i.e., signal applies). This matches D-09 "wake within short window of being discarded"; cold-start case = no prior discard timestamp, so the wake itself starts the bias trail.
       - Build `next: DomainBiasRecord` with: `domain`, `biasOffset = Math.min(AI_BIAS_MAX, (existing?.biasOffset ?? 0) + 0.1)` (weaker than Keep Alive's 0.2), `keepAliveCount = existing?.keepAliveCount ?? 0`, `misclassificationCount = (existing?.misclassificationCount ?? 0) + 1`, `updatedAt = Date.now()`.
       - Persist via `putDomainBias(next)`. Wrap in try/catch silent.

    6. Export a test-only helper `__resetOpenVisitMaps()` that clears both maps. Place behind a NODE_ENV check or simply export it for tests to call. (This keeps tests deterministic between cases.)

    Expand src/background/ai-learning.test.ts — replace `.todo` entries with real assertions. Use `vi.mock('./idb', ...)`. Tests required (keep Wave 0 infrastructure check):
       - `it('FR-06: recordKeepAlive increments biasOffset by 0.2 on a fresh domain')` — mock getDomainBias to return undefined; assert putDomainBias called with biasOffset === 0.2, keepAliveCount === 1.
       - `it('FR-06: recordKeepAlive clamps biasOffset to AI_BIAS_MAX')` — mock existing biasOffset = 0.95; expect new biasOffset === 1.0 (not 1.15).
       - `it('FR-06: recordKeepAlive increments keepAliveCount and writes updatedAt')` — mock existing keepAliveCount = 3; expect 4 written.
       - `it('FR-06: recordTabActivation opens a visit; closeTabVisit appends TabHistoryRecord with correct dwellMs')` — call recordTabActivation(tabId=1, ts=1000, url='https://github.com/repo'); spy on appendTabHistory; advance Date.now to 5000; closeTabVisit(1, false); assert appendTabHistory called with `expect.objectContaining({ domain: 'github.com', dwellMs: 4000, visitStart: 1000 })`.
       - `it('FR-06: closeTabVisit no-ops when no visit was opened')` — call closeTabVisit on unknown tabId; appendTabHistory not called.
       - `it('FR-06: closeTabVisit silently skips invalid URLs')` — recordTabActivation with url='not a url'; closeTabVisit; appendTabHistory not called.
       - `it('T-03-14: recordWakeMisclassification short-circuits when prior updatedAt is older than AI_WAKE_SIGNAL_WINDOW_MS')` — mock existing.updatedAt = Date.now() - (AI_WAKE_SIGNAL_WINDOW_MS + 1000); expect putDomainBias NOT called.
       - `it('FR-06: recordWakeMisclassification within window applies +0.1 bias')` — mock existing.updatedAt = Date.now() - 1000 (well within window); existing.biasOffset = 0.1; expect putDomainBias called with biasOffset ≈ 0.2, misclassificationCount incremented.
       - `it('FR-06: recordWakeMisclassification on cold-start (no existing record) writes initial bias')` — mock getDomainBias to return undefined; expect putDomainBias called with biasOffset === 0.1, misclassificationCount === 1.

       Use `__resetOpenVisitMaps()` in beforeEach to clear maps between tests.
  </action>
  <verify>
    <automated>npm test -- src/background/ai-learning.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/background/ai-learning.ts exports `recordKeepAlive`, `recordTabActivation`, `closeTabVisit`, `recordWakeMisclassification`, `__resetOpenVisitMaps`
    - src/background/ai-learning.ts imports `getDomainBias`, `putDomainBias`, `appendTabHistory` from `./idb`
    - src/background/ai-learning.ts contains module-level `openVisits` and `openUrls` Map declarations
    - recordKeepAlive clamps biasOffset using `Math.min(AI_BIAS_MAX, ...)` (verified by source assertion + test)
    - closeTabVisit appends a TabHistoryRecord and deletes openVisits + openUrls entries
    - recordWakeMisclassification checks `Date.now() - existing.updatedAt` against AI_WAKE_SIGNAL_WINDOW_MS
    - src/background/ai-learning.test.ts contains at least 9 real `it(` assertions (no `.todo`)
    - `npm test -- src/background/ai-learning.test.ts` exits 0
    - Full `npm test` suite exits 0
  </acceptance_criteria>
  <done>ai-learning.ts is implemented end-to-end with clamping, NaN-guard, visit tracking, and short-window misclassification signal. All behavioral tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend isDiscardable() with classification param + handleAlarmTick AI integration; expand hibernation.test.ts</name>
  <files>src/background/hibernation.ts, src/background/hibernation.test.ts</files>
  <read_first>
    - src/background/hibernation.ts (current Phase 2 isDiscardable signature, handleAlarmTick storage.get pattern, discard loop with try/catch)
    - src/background/hibernation.test.ts (current Phase 2 tests — makeTab factory, TIMEOUT_MS/FORM_PROTECTION_MS imports, EXPIRED helper)
    - src/background/classifier.ts (after Wave 1 — verify `classifyBatch` export)
    - src/shared/types.ts (ClassificationResult — already exported)
    - src/shared/constants.ts (AI_CONFIDENCE_THRESHOLD — already exported)
    - .planning/phases/03-ai-intelligence/03-PATTERNS.md (hibernation.ts section — AI integration block placement)
    - .planning/phases/03-ai-intelligence/03-RESEARCH.md (D-04..D-07 mapping table; isDiscardable AI integration code example)
  </read_first>
  <action>
    Per D-04 (Vital never hibernated), D-05 (Semi-Active 1.5×), D-06 (Dead 0.5×), D-07 (low-confidence fallback to base timeoutMs).

    1. src/background/hibernation.ts — extend isDiscardable signature:
       - Add optional 7th parameter `classification?: ClassificationResult` AFTER the existing 6 parameters (do NOT reorder).
       - Import `ClassificationResult` from `../shared/types` and `AI_CONFIDENCE_THRESHOLD` from `../shared/constants`.
       - Inside isDiscardable, AFTER the existing structural guards (active, audible, pinned, discarded, status, protectedTabs, protectedDomains, http(s) gate) and AFTER computing `lastActive`, BEFORE the existing `if (now - lastActive < timeoutMs) return false` line:
         - Insert the AI integration block:
           - If `classification && classification.label !== null && classification.confidence >= AI_CONFIDENCE_THRESHOLD`:
             - If `classification.label === 'Vital'` → return `false` (D-04).
             - If `classification.label === 'Semi-Active'` → if `now - lastActive < timeoutMs * 1.5` return `false`; otherwise let execution continue past the structural-form-activity check to return `true`. Implementation note: easiest is to overwrite a local `effectiveTimeoutMs` variable used by the subsequent inactivity check.
             - If `classification.label === 'Dead'` → set `effectiveTimeoutMs = timeoutMs * 0.5`.
           - Otherwise (cold start, low confidence, or undefined classification) — D-07: leave `effectiveTimeoutMs = timeoutMs` (base).
         - Refactor the existing `if (now - lastActive < timeoutMs) return false` to use the local `effectiveTimeoutMs`.
       - Preserve existing FORM_PROTECTION_MS check unchanged.

    2. src/background/hibernation.ts — extend handleAlarmTick:
       - Import `classifyBatch` and `ensureOffscreen` from `./classifier`.
       - Add `'ai_classifications'` to the existing atomic `chrome.storage.local.get([...])` keys array.
       - Parse `aiClassifications = (result['ai_classifications'] as Record<number, ClassificationResult>) ?? {}` after the existing destructuring.
       - BEFORE the per-tab discard loop:
         - Build `candidateTabs` by filtering the `tabs` result with the same pre-classification gates: id present, url starts with http(s), not active, not pinned, not audible, status complete, not in protectedTabs, hostname not in protectedDomains. (These mirror isDiscardable's structural guards. Avoid calling isDiscardable here so we can independently identify "candidates worth classifying" without coupling.)
         - If `candidateTabs.length > 0`: call `await classifyBatch(candidateTabs.map(t => ({ tabId: t.id!, url: t.url!, meta: tabMeta[t.id!] ?? { lastActiveAt: 0 } })))`. This call will populate ai_classifications storage. Wrap in try/catch — silent on error (offscreen may not be ready first tick).
         - Re-read ai_classifications from storage AFTER classifyBatch completes so the per-tab loop sees fresh labels: `const freshAi = await chrome.storage.local.get('ai_classifications'); const liveClassifications = (freshAi['ai_classifications'] as Record<number, ClassificationResult>) ?? aiClassifications`.
       - In the existing per-tab discard loop: pass `liveClassifications[tab.id!]` as the new 7th argument to `isDiscardable(...)`.

    3. src/background/hibernation.test.ts — append new describe blocks (preserve all existing tests):
       - `describe('isDiscardable AI integration (FR-07)')`:
         - `it('D-04: returns false for Vital classification regardless of inactivity')` — make a tab with `lastActiveAt = EXPIRED - 24*60*60*1000` (a day past timeout); pass classification `{ label: 'Vital', confidence: 0.9, cachedAt: NOW }`; expect false.
         - `it('D-05: returns false when Semi-Active and elapsed is between 1× and 1.5× timeout')` — `lastActive = NOW - timeoutMs * 1.2`; classification `{ label: 'Semi-Active', confidence: 0.8, ... }`; expect false.
         - `it('D-05: returns true when Semi-Active and elapsed exceeds 1.5× timeout')` — `lastActive = NOW - timeoutMs * 1.6`; expect true.
         - `it('D-06: returns false when Dead and elapsed is below 0.5× timeout')` — `lastActive = NOW - timeoutMs * 0.4`; classification Dead; expect false.
         - `it('D-06: returns true when Dead and elapsed exceeds 0.5× timeout')` — `lastActive = NOW - timeoutMs * 0.6`; classification Dead; expect true.
         - `it('D-07: low confidence falls back to base timeoutMs')` — classification `{ label: 'Vital', confidence: 0.3, ... }`; lastActive = NOW - timeoutMs / 2; expect false (under base timeout). Now lastActive = NOW - timeoutMs - 1000; expect true (past base timeout). Confirms Vital does NOT short-circuit when confidence is below threshold.
         - `it('D-07: undefined classification uses base timeoutMs')` — pass no classification arg; lastActive = NOW - timeoutMs / 2; expect false. lastActive = NOW - timeoutMs - 1000; expect true.
         - `it('D-07: null label falls back to base timeoutMs')` — classification `{ label: null, confidence: 0, ... }`; same expectations as undefined.
         - `it('FR-07: AI integration does not override structural guards (active/audible/pinned)')` — pass classification Dead with elapsed > 0.5× timeout AND tab.active = true; expect false (active wins).
  </action>
  <verify>
    <automated>npm test -- src/background/hibernation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/background/hibernation.ts isDiscardable accepts a 7th optional parameter typed as `ClassificationResult | undefined`
    - src/background/hibernation.ts isDiscardable contains the literal strings `'Vital'`, `'Semi-Active'`, `'Dead'`
    - src/background/hibernation.ts contains the literal token `AI_CONFIDENCE_THRESHOLD`
    - src/background/hibernation.ts contains the literal arithmetic `timeoutMs * 1.5` and `timeoutMs * 0.5`
    - src/background/hibernation.ts handleAlarmTick chrome.storage.local.get array contains `'ai_classifications'`
    - src/background/hibernation.ts imports `classifyBatch` from `./classifier`
    - src/background/hibernation.test.ts contains at least 9 new `it(` assertions in the AI integration describe
    - All Phase 1 + Phase 2 isDiscardable tests still pass (regression check)
    - `npm test -- src/background/hibernation.test.ts` exits 0
    - Full `npm test` suite exits 0
  </acceptance_criteria>
  <done>isDiscardable applies V/S/D multipliers and falls back correctly. handleAlarmTick now drives the classification path. All structural guards remain authoritative.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SW lifecycle — index.ts: top-level ensureOffscreen, behavioral hooks, KEEP_ALIVE handler, ai_install_date defaults; expand index.test.ts</name>
  <files>src/background/index.ts, src/background/index.test.ts</files>
  <read_first>
    - src/background/index.ts (current Phase 2 listeners — preserve every existing listener registration)
    - src/background/ai-learning.ts (after Task 1 — exports recordKeepAlive/recordTabActivation/closeTabVisit)
    - src/background/classifier.ts (after Wave 1 — ensureOffscreen)
    - src/background/index.test.ts (current Phase 1 tests — handleManualHibernate coverage)
    - .planning/phases/03-ai-intelligence/03-PATTERNS.md (index.ts behavioral hook section + onMessage extension)
    - .planning/phases/03-ai-intelligence/03-RESEARCH.md (Pattern 1 ensureOffscreen at top level)
  </read_first>
  <action>
    Per D-09 (behavioral signal sources), D-14 (KEEP_ALIVE doubles as override), T-03-12 (KEEP_ALIVE validation), Top-Level Synchronous Listener Registration rule.

    1. src/background/index.ts — imports (preserve existing imports):
       - `import { ensureOffscreen } from './classifier'`
       - `import { recordKeepAlive, recordTabActivation, closeTabVisit } from './ai-learning'`

    2. Top-level boot — directly after the existing `ensureHibernateAlarm()` call (which already runs at module top level), add a synchronous `ensureOffscreen()` call (wrap in `.catch(err => console.error(...))` to avoid unhandled promise rejections; the function itself is idempotent).

    3. onInstalled handler — extend existing install branch storage.set to include two new keys:
       - `ai_install_date: Date.now()`
       - `ai_classifications: {}`
       Extend the update branch backfill block to also include both keys (use the existing pattern: only set if undefined).

    4. chrome.tabs.onActivated — extend the existing listener WITHOUT removing the storage write:
       - After the existing tab_meta storage write, call `chrome.tabs.get(tabId, (tab) => { if (chrome.runtime.lastError || !tab?.url) return; recordTabActivation(tabId, Date.now(), tab.url) })` (fire-and-forget). This avoids requiring a synchronous url lookup.
       - Also: when a new tab activates, close the previous active tab's visit. Implementation: track `lastActiveTabId` in a module-level `let lastActiveTabId: number | null = null` variable (top-level). Before recording the new activation, if `lastActiveTabId !== null && lastActiveTabId !== tabId`, read `tab_meta[lastActiveTabId]?.lastFormActivity` and call `closeTabVisit(lastActiveTabId, !!lastFormActivity).catch(() => {})`. Then `lastActiveTabId = tabId`. This is acceptable because the SW guarantees the listener runs synchronously per onActivated event.

    5. chrome.tabs.onRemoved — extend the existing listener:
       - After the existing tab_meta delete + deleteThumbnail call, also call `closeTabVisit(tabId, false).catch(() => {})`. closeTabVisit reads form activity from its own openUrls Map snapshot and the SW does not have a sync handle on lastFormActivity at this point — acceptable per D-09 imprecision (form-activity signal is best-effort).
       - If `tabId === lastActiveTabId`, reset `lastActiveTabId = null`.

    6. chrome.runtime.onMessage — append a new branch AFTER the existing `CAPTURE_TAB` block, mirroring the existing branch shape, with T-03-12 validation:
       ```
       if (message.type === 'KEEP_ALIVE' && typeof message.tabId === 'number' && typeof message.domain === 'string' && message.domain.length < 256) {
         recordKeepAlive(message.tabId as number, message.domain as string).catch(() => {})
         return
       }
       ```
       Reject silently when validation fails.

    7. src/background/index.test.ts — append new describe block (preserve all existing tests):
       - `describe('SW Phase 3 wiring (FR-06)')`:
         - `it('onInstalled install branch sets ai_install_date and ai_classifications defaults')` — trigger onInstalled with `{ reason: 'install' }` via vitest-chrome's mockTriggerListener (or capture handler from vi.mocked); assert `chrome.storage.local.set` called with an object whose keys include `ai_install_date` (numeric) and `ai_classifications: {}`. May need to import index.ts in isolation; if the existing index.test.ts uses a separate import strategy, follow that pattern.
         - `it('KEEP_ALIVE message branch validates tabId and domain types')` — capture the onMessage handler (or use mockTriggerListener); send `{ type: 'KEEP_ALIVE', tabId: 5, domain: 'github.com' }`; spy on ai-learning recordKeepAlive (vi.mock('./ai-learning'))); assert called with (5, 'github.com').
         - `it('KEEP_ALIVE rejects non-number tabId silently')` — send `{ type: 'KEEP_ALIVE', tabId: 'oops', domain: 'github.com' }`; assert recordKeepAlive NOT called.
         - `it('KEEP_ALIVE rejects oversized domain (>=256 chars) silently')` — send a 300-char domain; assert recordKeepAlive NOT called.

         Existing handleManualHibernate tests remain unchanged.

    Note: the SW index.ts already runs at top level on import — index.test.ts must import index.ts within the test to trigger registration. If existing index.test.ts uses a re-import strategy with vi.resetModules(), follow it; otherwise add minimal scaffolding.
  </action>
  <verify>
    <automated>npm test -- src/background/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/background/index.ts imports `ensureOffscreen` from `./classifier`
    - src/background/index.ts imports `recordKeepAlive`, `recordTabActivation`, `closeTabVisit` from `./ai-learning`
    - src/background/index.ts contains a top-level `ensureOffscreen()` invocation (outside any function body)
    - src/background/index.ts onInstalled install branch storage.set contains literal `ai_install_date` and `ai_classifications`
    - src/background/index.ts contains the literal token `KEEP_ALIVE`
    - src/background/index.ts KEEP_ALIVE branch validates `typeof message.tabId === 'number'` AND `typeof message.domain === 'string'` AND length check
    - src/background/index.ts onActivated handler invokes `recordTabActivation` (grep for the literal token)
    - src/background/index.ts onRemoved handler invokes `closeTabVisit` (grep for the literal token)
    - src/background/index.test.ts contains at least 4 new `it(` assertions in the Phase 3 wiring describe (covering install defaults, KEEP_ALIVE valid path, invalid tabId, oversized domain)
    - `npm test -- src/background/index.test.ts` exits 0
    - Full `npm test` suite exits 0 with no Phase 1/2 regressions
  </acceptance_criteria>
  <done>SW boot warms the Offscreen Document; behavioral events flow into IndexedDB; KEEP_ALIVE messages update domain bias; storage defaults are populated for FR-06 countdown display.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `npm test` exits 0 with all new behavioral tests reported as green.
2. `grep -c "ensureOffscreen" src/background/index.ts` returns at least 2 (import + invocation).
3. `grep -c "KEEP_ALIVE\\|recordKeepAlive" src/background/index.ts` returns at least 2.
4. `grep -c "Vital\\|Semi-Active\\|Dead" src/background/hibernation.ts` returns at least 3.
5. `grep -c "ai_classifications" src/background/hibernation.ts` returns at least 1.
6. `grep -c "ai_install_date" src/background/index.ts` returns at least 2 (install + update branches).
</verification>

<success_criteria>
Wave 2 complete when:
- Behavioral events (activation, deactivation, removal) populate tab-history in IndexedDB
- Keep Alive and wake-misclassification both write clamped bias values
- isDiscardable correctly applies V/S/D multipliers and falls back to base timeout on low confidence / cold start
- handleAlarmTick triggers classifyBatch and reads fresh ai_classifications before each tab decision
- SW boot warms the offscreen document
- ai_install_date is written on install for FR-06 countdown
- All tests green; no Phase 1/2 regression
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-intelligence/03-03-SUMMARY.md` per the standard summary template.
</output>
