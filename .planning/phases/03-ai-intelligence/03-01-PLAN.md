---
phase: 03-ai-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - manifest.json
  - vite.config.ts
  - vitest.setup.ts
  - src/shared/types.ts
  - src/shared/constants.ts
  - scripts/generate-model.py
  - src/assets/classifier.onnx
  - src/offscreen/index.html
  - src/background/classifier.test.ts
  - src/background/ai-learning.test.ts
  - src/offscreen/main.test.ts
autonomous: true
requirements:
  - FR-05
  - FR-06
  - FR-07
  - NFR-02
  - NFR-03
  - NFR-04
user_setup:
  - service: python-ml-tooling
    why: "One-time generation of classifier.onnx via scikit-learn + skl2onnx. The .onnx file is committed; users do NOT need Python after Wave 0 unless regenerating the model."
    env_vars: []
    dashboard_config:
      - task: "Install Python deps before running scripts/generate-model.py"
        location: "Local shell — pip install scikit-learn skl2onnx onnx numpy"

must_haves:
  truths:
    - "npm install succeeds with onnxruntime-web and vite-plugin-static-copy added to package.json"
    - "manifest.json has 'offscreen' permission and 'wasm-unsafe-eval' in extension_pages CSP"
    - "manifest.json web_accessible_resources covers src/offscreen/index.html, ort/*.wasm, ort/*.mjs, src/assets/classifier.onnx"
    - "vite.config.ts rollupOptions.input contains both dashboard and offscreen entries"
    - "vite.config.ts uses viteStaticCopy to copy node_modules/onnxruntime-web/dist/*.wasm and *.mjs into ort/"
    - "src/shared/types.ts exports TabVitality union, ClassificationResult, TabHistoryRecord, DomainBiasRecord; StorageSchema includes ai_classifications and ai_install_date"
    - "src/shared/constants.ts exports AI_CONFIDENCE_THRESHOLD, AI_COLD_START_MIN_SAMPLES, AI_HISTORY_WINDOW_MS, AI_WAKE_SIGNAL_WINDOW_MS, AI_BIAS_MAX, AI_LEARNING_DAYS, VITAL_DOMAINS, DEAD_DOMAINS"
    - "src/assets/classifier.onnx exists and is under 100 KB"
    - "scripts/generate-model.py exists and is runnable with the documented Python deps"
    - "npm test exits 0 with the three new test stub files present and all existing tests still green"
  artifacts:
    - path: "package.json"
      provides: "onnxruntime-web + vite-plugin-static-copy dependencies"
      contains: "onnxruntime-web"
    - path: "manifest.json"
      provides: "offscreen permission, wasm-unsafe-eval CSP, web_accessible_resources for ort and classifier.onnx"
      contains: "offscreen"
    - path: "vite.config.ts"
      provides: "offscreen entry + viteStaticCopy for WASM/MJS"
      contains: "offscreen"
    - path: "vitest.setup.ts"
      provides: "Test shims for chrome.offscreen + chrome.runtime.getContexts + ContextType"
      contains: "chrome.offscreen"
    - path: "src/shared/types.ts"
      provides: "Phase 3 type definitions"
      contains: "TabVitality"
    - path: "src/shared/constants.ts"
      provides: "Phase 3 numeric/threshold constants"
      contains: "AI_CONFIDENCE_THRESHOLD"
    - path: "scripts/generate-model.py"
      provides: "Offline ONNX model generator (Wave 0 prerequisite — committed artifact)"
      contains: "DecisionTreeClassifier"
    - path: "src/assets/classifier.onnx"
      provides: "Bundled synthetic Decision Tree ONNX model (committed binary asset)"
    - path: "src/offscreen/index.html"
      provides: "Placeholder HTML file so vite build does not fail; replaced in Wave 1 plan 03-02"
    - path: "src/background/classifier.test.ts"
      provides: "Wave 0 stub for FR-05 feature vector + FR-07 cold start"
    - path: "src/background/ai-learning.test.ts"
      provides: "Wave 0 stub for FR-06 Keep Alive bias write + wake misclassification window"
    - path: "src/offscreen/main.test.ts"
      provides: "Wave 0 stub for FR-05 CLASSIFY_BATCH handler + NFR-04 zero-external-fetch invariant"
  key_links:
    - from: "manifest.json permissions"
      to: "chrome.offscreen.createDocument"
      via: "offscreen permission entry"
      pattern: "offscreen"
    - from: "manifest.json content_security_policy"
      to: "WASM compilation in offscreen document"
      via: "wasm-unsafe-eval source"
      pattern: "wasm-unsafe-eval"
    - from: "manifest.json web_accessible_resources"
      to: "ort.env.wasm.wasmPaths = chrome.runtime.getURL('ort/')"
      via: "ort/*.wasm + ort/*.mjs entries"
      pattern: "ort/.*wasm"
    - from: "vite.config.ts viteStaticCopy"
      to: "dist/ort/ output directory"
      via: "node_modules/onnxruntime-web/dist/*.wasm copy target"
      pattern: "onnxruntime-web/dist"
    - from: "src/shared/types.ts StorageSchema"
      to: "chrome.storage.local.get('ai_classifications')"
      via: "ai_classifications key"
      pattern: "ai_classifications"
---

<objective>
Wave 0 (foundation) for Phase 3 AI Intelligence. Everything downstream depends on these artifacts being in place BEFORE Wave 1 code lands.

Deliverables:
1. Add `onnxruntime-web@^1.26.0` (dependency) and `vite-plugin-static-copy@^4.1.0` (devDependency) to package.json.
2. Update manifest.json: add `offscreen` permission, add `wasm-unsafe-eval` to extension_pages CSP, register `src/offscreen/index.html` + `ort/*.wasm` + `ort/*.mjs` + `src/assets/classifier.onnx` in web_accessible_resources.
3. Update vite.config.ts: add `offscreen` entry to rollupOptions.input AND wire viteStaticCopy plugin to copy ORT WASM/MJS files into `dist/ort/`.
4. Create a placeholder `src/offscreen/index.html` so vite build does not fail in Wave 0 (replaced with the real entry in Wave 1).
5. Extend vitest.setup.ts: add shims for `chrome.offscreen`, `chrome.runtime.getContexts`, and `chrome.runtime.ContextType` so SW-side classifier wiring can be unit-tested without a real Chrome host.
6. Extend src/shared/types.ts: add `TabVitality`, `ClassificationResult`, `TabHistoryRecord`, `DomainBiasRecord` interfaces. Extend `StorageSchema` with `ai_classifications: Record<number, ClassificationResult>` and `ai_install_date: number`.
7. Extend src/shared/constants.ts: add `AI_CONFIDENCE_THRESHOLD`, `AI_COLD_START_MIN_SAMPLES`, `AI_HISTORY_WINDOW_MS`, `AI_WAKE_SIGNAL_WINDOW_MS`, `AI_BIAS_MAX`, `AI_LEARNING_DAYS`, `VITAL_DOMAINS`, `DEAD_DOMAINS`.
8. Create `scripts/generate-model.py` — committed Python script that emits a small Decision Tree ONNX model from a synthetic rule-based dataset. Run it locally to produce `src/assets/classifier.onnx` (also committed).
9. Create three structural test stub files (`classifier.test.ts`, `ai-learning.test.ts`, `offscreen/main.test.ts`) — Wave 0 stubs assert only on constants and shims; behavioural specs are `it.todo` placeholders filled by Waves 1-3.

Purpose: Every Wave 1+ task depends on these files being correct. Cold-start the ONNX model so downstream agents never need Python again.

Output: Updated config + types + constants, generated ONNX asset, three test stubs. `npm test` exits 0 with no regressions.
</objective>

<execution_context>
@/home/lizzyman04/.claude/get-shit-done/workflows/execute-plan.md
@/home/lizzyman04/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ai-intelligence/03-CONTEXT.md
@.planning/phases/03-ai-intelligence/03-RESEARCH.md
@.planning/phases/03-ai-intelligence/03-PATTERNS.md
@.planning/phases/03-ai-intelligence/03-VALIDATION.md
@.planning/phases/02-ui-and-rich-previews/02-CONTEXT.md

<interfaces>
Current state of files being modified — read before touching.

From src/shared/types.ts (Phase 2 — append-only, do not remove existing exports):
```typescript
export interface TabMeta { lastActiveAt: number; lastFormActivity?: number }
export interface HibernationEvent { timestamp: number; tabId: number; url: string }
export interface StorageSchema {
  hibernation_enabled: boolean
  hibernated_count: number
  tab_meta: Record<number, TabMeta>
  protected_tabs: number[]
  protected_domains: string[]
  timeout_minutes: number
  hibernation_events: HibernationEvent[]
}
```

From src/shared/constants.ts (Phase 2 — append-only):
```typescript
export const ALARM_NAME = 'HIBERNATE_CHECK' as const
export const TIMEOUT_MS = 45 * 60 * 1000
export const FORM_PROTECTION_MS = 5 * 60 * 1000
export const DEFAULT_TIMEOUT_MINUTES = 45
export const RAM_PER_TAB_MB = 150
export const THUMBNAIL_MAX_SIZE_BYTES = 250 * 1024
export const IDB_SIZE_CAP_BYTES = 25 * 1024 * 1024
```

From manifest.json (Phase 2 — modify only specified keys; preserve everything else):
- permissions: ["storage", "tabs", "alarms", "contextMenus", "scripting", "activeTab"]
- web_accessible_resources: [{ "resources": ["src/dashboard/index.html"], "matches": ["<all_urls>"] }]
- content_security_policy.extension_pages: "script-src 'self'; object-src 'self'; style-src 'self' 'unsafe-inline'"

From vite.config.ts (Phase 2):
- plugins: [react(), tailwindcss(), crx({ manifest })]
- build.rollupOptions.input: { dashboard: resolve(__dirname, 'src/dashboard/index.html') }

From vitest.setup.ts (Phase 2 — Object.assign(global, { chrome: { ...chrome, action: chromeMV3Action } }) pattern):
- Existing shims: chromeMV3Action (setBadgeText, etc.), ResizeObserverStub, fake-indexeddb/auto
- Phase 3 ADDS: chrome.offscreen shim, chrome.runtime.getContexts shim, chrome.runtime.ContextType shim
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline -> Extension binary | WASM files and ONNX model are bundled at build time; runtime cannot fetch them remotely (CSP) |
| Offscreen Document context -> External network | NFR-04: zero telemetry; the Offscreen Document must NEVER call fetch() with external URLs (enforced in plan 03-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | scripts/generate-model.py output (classifier.onnx) | LOW | accept | Synthetic rule-based training data; no PII; committed artifact reviewed in PR; model is a deterministic transform of rule labels with fixed numpy random seed |
| T-03-02 | Elevation of Privilege | manifest.json CSP wasm-unsafe-eval scope | LOW | mitigate | Scope wasm-unsafe-eval to extension_pages only; web_accessible_resources restricted to ort/*.wasm, ort/*.mjs, src/offscreen/index.html, src/assets/classifier.onnx — no wildcards or external origins |
| T-03-03 | Information Disclosure | ORT-Web WASM file path leak via getURL | LOW | accept | chrome.runtime.getURL() emits extension-id URLs that are inherently exposed; no secrets in the path |
| T-03-04 | Tampering | Test setup chrome.offscreen shim | LOW | mitigate | Shim returns predictable values only; never invokes real chrome.offscreen API; isolated to vitest.setup.ts |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add ORT and vite-plugin-static-copy deps; wire vite.config.ts and manifest.json for offscreen + WASM serving</name>
  <files>package.json, manifest.json, vite.config.ts, src/offscreen/index.html</files>
  <read_first>
    - package.json (current dependencies and devDependencies — preserve every existing entry verbatim)
    - manifest.json (current permissions array, web_accessible_resources array, content_security_policy — append only, do not replace)
    - vite.config.ts (current plugins array, rollupOptions.input — preserve dashboard entry)
    - .planning/phases/03-ai-intelligence/03-RESEARCH.md (Pattern 5 + viteStaticCopy code example + Pitfall 6 CSP)
    - .planning/phases/03-ai-intelligence/03-PATTERNS.md (Config File Changes Required section)
  </read_first>
  <action>
    Per D-01..D-14 (offscreen architecture), per RESEARCH.md Pattern 5 and Pitfall 6.

    1. package.json: add the literal dependency entry `"onnxruntime-web": "^1.26.0"` to the dependencies object; add `"vite-plugin-static-copy": "^4.1.0"` to the devDependencies object. Preserve every existing dep verbatim. Run `npm install` after editing so package-lock.json updates.

    2. manifest.json:
       - permissions: append the literal string `"offscreen"` to the existing permissions array. Final permissions array must contain: storage, tabs, alarms, contextMenus, scripting, activeTab, offscreen.
       - web_accessible_resources: append three new entries, each shaped as `{ "resources": [...], "matches": ["<all_urls>"] }`:
         - resources `["src/offscreen/index.html"]`
         - resources `["ort/*.wasm", "ort/*.mjs"]`
         - resources `["src/assets/classifier.onnx"]`
       - content_security_policy.extension_pages: change the existing value to `"script-src 'self' 'wasm-unsafe-eval'; object-src 'self'; style-src 'self' 'unsafe-inline'"`. The only delta is inserting `'wasm-unsafe-eval'` into script-src; do not touch object-src or style-src.

    3. vite.config.ts:
       - import viteStaticCopy from `vite-plugin-static-copy` at the top of the file.
       - In the plugins array, after `crx({ manifest })`, add a `viteStaticCopy({ targets: [...] })` call with TWO targets: `{ src: 'node_modules/onnxruntime-web/dist/*.wasm', dest: 'ort' }` and `{ src: 'node_modules/onnxruntime-web/dist/*.mjs', dest: 'ort' }`.
       - In `build.rollupOptions.input`, add the key `offscreen: resolve(__dirname, 'src/offscreen/index.html')` alongside the existing `dashboard:` entry. Do not remove the dashboard entry.

    4. Create `src/offscreen/index.html` as a single-line placeholder so `vite build` succeeds even before Wave 1 lands the real entry. Plan 03-02 replaces this file. Content: `<!-- placeholder for Wave 1 plan 03-02 -->`.

    Do not touch any other manifest key, vite plugin, or package field.
  </action>
  <verify>
    <automated>npm install &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains the literal token `"onnxruntime-web"` with a value matching `^1.26`
    - package.json devDependencies contains the literal token `"vite-plugin-static-copy"` with a value matching `^4.1`
    - `npm install` exits 0 and updates package-lock.json
    - manifest.json permissions array contains the literal string `"offscreen"` (grep `offscreen` in manifest.json returns a match inside the permissions array)
    - manifest.json content_security_policy.extension_pages contains the literal substring `wasm-unsafe-eval`
    - manifest.json web_accessible_resources contains entries whose resources include `src/offscreen/index.html`, `ort/*.wasm`, `ort/*.mjs`, and `src/assets/classifier.onnx`
    - vite.config.ts contains an import statement referencing `vite-plugin-static-copy`
    - vite.config.ts plugins array includes a `viteStaticCopy(` call with copy targets for both `*.wasm` and `*.mjs` (both pointing at `dest: 'ort'`)
    - vite.config.ts rollupOptions.input contains the key `offscreen` resolving to `src/offscreen/index.html`
    - src/offscreen/index.html exists as a file (placeholder content is acceptable for this task; Wave 1 replaces it)
    - `npm test` exits 0 — every existing test still green; no new failures
  </acceptance_criteria>
  <done>Build tooling is wired for ORT-Web bundling and offscreen entry. CSP allows WASM compilation. WASM files copy to dist/ort/ on build. No existing test regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Extend types.ts + constants.ts; extend vitest.setup.ts with chrome.offscreen/getContexts shims</name>
  <files>src/shared/types.ts, src/shared/constants.ts, vitest.setup.ts</files>
  <read_first>
    - src/shared/types.ts (current TabMeta, HibernationEvent, StorageSchema, getStorage, setStorage exports — append only)
    - src/shared/constants.ts (current export list — append only)
    - vitest.setup.ts (current chrome mock injection pattern — extend the global.chrome assignment)
    - .planning/phases/03-ai-intelligence/03-PATTERNS.md (types.ts and constants.ts sections — concrete shapes)
    - .planning/phases/03-ai-intelligence/03-RESEARCH.md (Pattern 1 ensureOffscreen — chrome.offscreen.Reason.WORKERS + chrome.runtime.ContextType.OFFSCREEN_DOCUMENT shapes needed by test shim)
  </read_first>
  <action>
    Per D-03 (ai_classifications storage key), D-07 (AI_CONFIDENCE_THRESHOLD = 0.6), D-12/D-13 (V/S/D types must exist), and RESEARCH.md Pattern 1.

    1. src/shared/types.ts — APPEND only (do not modify existing exports):
       - `export type TabVitality = 'Vital' | 'Semi-Active' | 'Dead'`
       - `export interface ClassificationResult` with fields: `label: TabVitality | null`, `confidence: number`, `cachedAt: number`
       - `export interface TabHistoryRecord` with fields: `id?: number`, `domain: string`, `url: string`, `visitStart: number`, `visitEnd: number`, `dwellMs: number`, `hadFormActivity: boolean`, `timestamp: number`
       - `export interface DomainBiasRecord` with fields: `domain: string`, `biasOffset: number`, `keepAliveCount: number`, `misclassificationCount: number`, `updatedAt: number`
       - Extend the existing `StorageSchema` interface by adding TWO new fields inline: `ai_classifications: Record<number, ClassificationResult>` and `ai_install_date: number`. Keep every existing StorageSchema field unchanged.

    2. src/shared/constants.ts — APPEND only (do not modify existing constants), per RESEARCH.md Code Examples + Pitfall 5:
       - `AI_CONFIDENCE_THRESHOLD = 0.6` (D-07)
       - `AI_COLD_START_MIN_SAMPLES = 50` (Pitfall 5 cold-start floor)
       - `AI_HISTORY_WINDOW_MS = 14 * 24 * 60 * 60 * 1000` (D-03 14-day window)
       - `AI_WAKE_SIGNAL_WINDOW_MS = 5 * 60 * 1000` (5-minute wake = misclassification window)
       - `AI_BIAS_MAX = 1.0` (clamp ceiling)
       - `AI_LEARNING_DAYS = 14` (FR-06 countdown display)
       - `VITAL_DOMAINS: readonly string[] = ['github.com', 'docs.google.com', 'notion.so', 'linear.app', 'figma.com']` (D-02 preset)
       - `DEAD_DOMAINS: readonly string[] = []` (D-02 — none preset; user-driven via Keep Alive negative space)

    3. vitest.setup.ts — extend the global chrome injection so unit tests calling chrome.offscreen.* or chrome.runtime.getContexts() do not throw. Immediately after the existing `Object.assign(global, { chrome: ... })` block, attach additional shims onto the now-global `chrome` object (use a follow-up Object.assign or direct property writes). Required shims:
       - `chrome.offscreen = { createDocument: vi.fn().mockResolvedValue(undefined), closeDocument: vi.fn().mockResolvedValue(undefined), Reason: { WORKERS: 'WORKERS', BLOBS: 'BLOBS' } }`
       - `chrome.runtime.getContexts = vi.fn().mockResolvedValue([])`
       - `chrome.runtime.ContextType = { OFFSCREEN_DOCUMENT: 'OFFSCREEN_DOCUMENT' }`
       - Guarded `chrome.runtime.getURL` shim: only assign if not already a function; default returns `'chrome-extension://test/' + path`.

    All changes are additive. Do not delete or rewrite any existing field, constant, or shim.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/types.ts contains the literal `export type TabVitality = 'Vital' | 'Semi-Active' | 'Dead'`
    - src/shared/types.ts contains `export interface ClassificationResult` with all three fields (label, confidence, cachedAt)
    - src/shared/types.ts contains `export interface TabHistoryRecord` with id?, domain, url, visitStart, visitEnd, dwellMs, hadFormActivity, timestamp
    - src/shared/types.ts contains `export interface DomainBiasRecord` with all five fields
    - src/shared/types.ts StorageSchema declaration contains the field names `ai_classifications` and `ai_install_date`
    - src/shared/constants.ts exports `AI_CONFIDENCE_THRESHOLD` equal to 0.6
    - src/shared/constants.ts exports `AI_COLD_START_MIN_SAMPLES` equal to 50
    - src/shared/constants.ts exports `AI_HISTORY_WINDOW_MS` equal to `14 * 24 * 60 * 60 * 1000`
    - src/shared/constants.ts exports `AI_WAKE_SIGNAL_WINDOW_MS` equal to `5 * 60 * 1000`
    - src/shared/constants.ts exports `AI_BIAS_MAX`, `AI_LEARNING_DAYS`, `VITAL_DOMAINS`, `DEAD_DOMAINS`
    - VITAL_DOMAINS array contains at least the strings `'github.com'` and `'docs.google.com'`
    - vitest.setup.ts contains the token `chrome.offscreen` or an assignment that adds an offscreen object onto the chrome global
    - vitest.setup.ts contains a stringified `'WORKERS'` value associated with offscreen.Reason
    - vitest.setup.ts contains `getContexts` and `ContextType` shim definitions
    - `npm test` exits 0; the existing 39 tests remain green
  </acceptance_criteria>
  <done>Phase 3 types and constants are exported and importable. Test environment can fake-call chrome.offscreen and chrome.runtime.getContexts without throwing. Existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Generate classifier.onnx; create scripts/generate-model.py; create three Wave 0 test stub files</name>
  <files>scripts/generate-model.py, src/assets/classifier.onnx, src/background/classifier.test.ts, src/background/ai-learning.test.ts, src/offscreen/main.test.ts</files>
  <read_first>
    - .planning/phases/03-ai-intelligence/03-RESEARCH.md (Code Examples: "Offline ONNX Model Generation" Python script + sklearn FloatTensorType shape + Pitfall 7 model output shape)
    - .planning/phases/03-ai-intelligence/03-PATTERNS.md ("Test File Structure (vitest + vitest-chrome)" shared pattern)
    - src/background/idb.test.ts (analog — Wave 0 stub pattern used in Phase 2; note "Wave 0 infrastructure check" idiom)
    - src/background/thumbnail.test.ts (analog — Wave 0 stub pattern with vi.mock('./idb'))
    - src/background/hibernation.test.ts (analog — makeTab factory + .toBe assertions style)
    - src/shared/constants.ts (must already contain Wave 0 constants from Task 2 — stub assertions reference AI_COLD_START_MIN_SAMPLES, AI_CONFIDENCE_THRESHOLD, AI_BIAS_MAX, AI_WAKE_SIGNAL_WINDOW_MS)
  </read_first>
  <action>
    Per RESEARCH.md "Offline ONNX Model Generation" pattern, per VALIDATION.md Wave 0 gap list, per D-10 (synthetic rule-based model — no real training data).

    1. Create `scripts/generate-model.py`:
       - Top-of-file docstring explaining the script generates `src/assets/classifier.onnx`, required Python deps (scikit-learn, skl2onnx, onnx, numpy), and the one-time-run nature.
       - Imports: numpy as np, sklearn.tree.DecisionTreeClassifier, skl2onnx.convert_sklearn, skl2onnx.common.data_types.FloatTensorType.
       - Generate a synthetic dataset of approximately 600 rows of 6-float-feature vectors with labels 0=Dead, 1=Semi-Active, 2=Vital using deterministic rules: high revisitFreq + high dwell + form activity = Vital; mid values = Semi-Active; low recency + no form + no boost = Dead. Use `np.random.default_rng(seed=42)` for determinism so committed .onnx file is reproducible.
       - Train `DecisionTreeClassifier(max_depth=5, random_state=42)`.
       - Export with `initial_types = [('float_input', FloatTensorType([None, 6]))]` and `options={id(clf): {'zipmap': False}}` so the model emits raw probabilities (output name `output_probability`) rather than a sklearn ZipMap dict — see RESEARCH.md Pitfall 7.
       - Write to `src/assets/classifier.onnx`. Print resulting model file size to stdout for sanity.

    2. Run the Python script locally with `python scripts/generate-model.py` (one-time) to produce `src/assets/classifier.onnx`. Commit BOTH the script and the .onnx artifact. If the directory `src/assets/` does not exist, create it. If Python deps are missing, document the `pip install` command in commit message — do not block the task on it; the .onnx artifact is what downstream waves consume.

    3. Create `src/background/classifier.test.ts` — Wave 0 stub. The classifier module does not yet exist (created in Wave 1 plan 03-02). Use the analog pattern from `idb.test.ts`: only import what already exists (constants from `../shared/constants`), assert on the cold-start threshold and confidence threshold values, and add `it.todo(...)` placeholders for the behavioral tests Wave 1+ will fill in. Concrete Wave 0 tests:
       - `it('Wave 0 infrastructure check: AI_COLD_START_MIN_SAMPLES is defined and is a positive integer')` — import from shared/constants, assert `AI_COLD_START_MIN_SAMPLES > 0 && Number.isInteger(AI_COLD_START_MIN_SAMPLES)`
       - `it('Wave 0 infrastructure check: AI_CONFIDENCE_THRESHOLD is between 0 and 1')`
       - `it.todo('FR-05: buildFeaturesForTab returns a 6-element number array for a tab with history')`
       - `it.todo('FR-07: buildFeaturesForTab returns null when total tab-history rows are below AI_COLD_START_MIN_SAMPLES')`
       - `it.todo('FR-05: classifyBatch sends CLASSIFY_BATCH message via chrome.runtime.sendMessage and caches result in chrome.storage.local')`

    4. Create `src/background/ai-learning.test.ts` — Wave 0 stub. Same structure: import only existing constants/types. Concrete tests:
       - `it('Wave 0 infrastructure check: AI_BIAS_MAX is 1.0 and AI_WAKE_SIGNAL_WINDOW_MS is 5 minutes')` — assert `AI_BIAS_MAX === 1.0` and `AI_WAKE_SIGNAL_WINDOW_MS === 5 * 60 * 1000`
       - `it.todo('FR-06: recordKeepAlive increments biasOffset by 0.2 clamped to AI_BIAS_MAX')`
       - `it.todo('FR-06: recordKeepAlive increments keepAliveCount and writes updatedAt')`
       - `it.todo('FR-06: recordWakeMisclassification only applies bias when wake is within AI_WAKE_SIGNAL_WINDOW_MS')`
       - `it.todo('FR-06: closeTabVisit appends a TabHistoryRecord to the tab-history store')`

    5. Create `src/offscreen/main.test.ts` — Wave 0 stub. The offscreen module does not yet exist. Concrete tests:
       - `it('Wave 0 infrastructure check: chrome.offscreen is shimmed in vitest.setup.ts')` — `expect(typeof (chrome as any).offscreen?.createDocument).toBe('function')`
       - `it('Wave 0 infrastructure check: chrome.runtime.getContexts is shimmed')` — `expect(typeof chrome.runtime.getContexts).toBe('function')`
       - `it.todo('FR-05: CLASSIFY_BATCH handler returns { results: [...] } shape with label and confidence per tabId')`
       - `it.todo('NFR-04: no fetch() call with external (non chrome-extension://) URL is ever made from offscreen/main.ts')`
       - `it.todo('FR-05: getSession reuses the same InferenceSession across multiple classify calls')`

    All three test files MUST pass under `npm test` immediately — `.todo` calls are reported as pending but do not fail the suite.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/generate-model.py` exists and contains the tokens `DecisionTreeClassifier` and `convert_sklearn`
    - scripts/generate-model.py uses `np.random.default_rng(seed=42)` or `random_state=42` so the artifact is reproducible
    - scripts/generate-model.py uses `FloatTensorType([None, 6])` (6 input features)
    - scripts/generate-model.py disables ZipMap via `options={id(clf): {'zipmap': False}}` (Pitfall 7)
    - File `src/assets/classifier.onnx` exists and its file size is greater than 0 bytes and less than 102400 bytes (100 KB)
    - File `src/background/classifier.test.ts` exists and contains the token `Wave 0 infrastructure check`
    - File `src/background/classifier.test.ts` contains at least 2 `it(` (real) assertions and at least 3 `it.todo(` entries
    - File `src/background/ai-learning.test.ts` exists and contains the literal token `AI_BIAS_MAX`
    - File `src/offscreen/main.test.ts` exists and contains the literal token `chrome.offscreen` and `getContexts`
    - `npm test` exits 0; vitest reports the new tests as part of the run; existing 39 tests remain green
  </acceptance_criteria>
  <done>Synthetic ONNX classifier is generated and committed. Three Wave 0 test stubs exist; npm test green. Wave 1 has everything it needs to create the real classifier.ts, ai-learning.ts, and offscreen/main.ts modules.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `npm install` succeeds and onnxruntime-web is present in node_modules.
2. `npm test` exits 0 with no regressions; vitest reports the three new test files.
3. `node -e "const m = require('./manifest.json'); console.log(m.permissions, m.content_security_policy.extension_pages)"` shows `offscreen` permission and `wasm-unsafe-eval` CSP.
4. `ls -la src/assets/classifier.onnx` shows a file under 100 KB.
5. `grep -c "AI_CONFIDENCE_THRESHOLD\|AI_COLD_START_MIN_SAMPLES" src/shared/constants.ts` returns at least 2.
</verification>

<success_criteria>
Phase 3 Wave 0 (this plan) is complete when:
- ORT-Web + vite-plugin-static-copy are installed and configured
- manifest.json supports offscreen + WASM via CSP and web_accessible_resources
- vite.config.ts builds the offscreen entry and copies ORT WASM/MJS to dist/ort/
- All Phase 3 types and constants are exported from src/shared/
- scripts/generate-model.py is committed; src/assets/classifier.onnx is committed and under 100 KB
- Three Wave 0 test stubs exist and are green
- npm test exits 0; no Phase 1/2 test regresses
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-intelligence/03-01-SUMMARY.md` per the standard summary template.
</output>
