---
phase: 05-polishing-launch
plan: 05
type: execute
wave: 2
depends_on: [01, 03, 04]
files_modified:
  - PRIVACY.md
  - docs/MEMORY-RUNBOOK.md
  - docs/CROSS-OS-SCREENSHOTS.md
  - docs/STORE-LISTING.md
  - README.md
autonomous: false
requirements: [NFR-01, NFR-04, COMP-02]
must_haves:
  truths:
    - "PRIVACY.md states the zero-telemetry guarantee (NFR-04) with the verified evidence (no external fetch in src/)"
    - "docs/MEMORY-RUNBOOK.md gives a reproducible Chrome Task Manager procedure to read SW + offscreen + pages RAM before vs after idle teardown"
    - "A human-verified Task Manager reading confirms total extension RAM < 45MB under many-tabs load after the idle window (NFR-01 gate of record)"
    - "docs/CROSS-OS-SCREENSHOTS.md is a per-OS checklist and screenshots are captured on Windows, macOS, and Linux (success criterion 2 gate)"
    - "Store listing copy (description, feature bullets) and README/LICENSE are launch-ready"
  artifacts:
    - path: "PRIVACY.md"
      provides: "Zero-telemetry privacy statement for CWS + open-source trust"
      contains: "telemetry"
    - path: "docs/MEMORY-RUNBOOK.md"
      provides: "Chrome Task Manager NFR-01 gate-of-record procedure"
      contains: "Task Manager"
    - path: "docs/CROSS-OS-SCREENSHOTS.md"
      provides: "Per-OS manual screenshot checklist (Win/macOS/Linux)"
      contains: "macOS"
    - path: "docs/STORE-LISTING.md"
      provides: "CWS marketing description + feature bullets + screenshot/promo-tile plan"
      contains: "feature"
  key_links:
    - from: "docs/MEMORY-RUNBOOK.md"
      to: "NFR-01 ≤45MB gate of record"
      via: "before/after idle-teardown Task Manager reading procedure"
      pattern: "45"
    - from: "PRIVACY.md"
      to: "NFR-04 zero telemetry"
      via: "documented evidence: no external fetch in src/"
      pattern: "telemetry"
---

<objective>
Ship the launch documentation and execute the two manual verification gates that cannot be automated. Docs: PRIVACY.md (D-10, zero-telemetry), the per-permission PERMISSIONS.md is delivered in plan 03, the Chrome Task Manager memory runbook (D-02 gate of record), the per-OS screenshot checklist (D-13 verify half), store-listing copy + screenshots (D-11), and README/LICENSE polish (D-12). Manual gates: the NFR-01 Task Manager reading (the pass/fail gate of record per D-02) and the per-OS screenshot pass (success criterion 2 per D-13) — both inherently manual (CWS measurement is a Task Manager reading; CI is single-OS and cannot render real per-OS UI).

Purpose: Complete the CWS launch package and empirically confirm the two headline Phase 5 success criteria (≤45MB RAM, cross-OS UI consistency) that automated tests cannot prove.
Output: PRIVACY.md, docs/MEMORY-RUNBOOK.md, docs/CROSS-OS-SCREENSHOTS.md, docs/STORE-LISTING.md, refreshed README.md, plus a human-verified memory reading and per-OS screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polishing-launch/05-CONTEXT.md
@.planning/phases/05-polishing-launch/05-RESEARCH.md
@.planning/phases/05-polishing-launch/05-PATTERNS.md
@.planning/phases/05-polishing-launch/05-01-PLAN.md
@.planning/phases/05-polishing-launch/05-03-PLAN.md
@.planning/phases/05-polishing-launch/05-04-PLAN.md

<interfaces>
<!-- Depends on plan 01 (idle teardown exists, so the runbook can measure before/after), plan 03 (PERMISSIONS.md + packaging), plan 04 (CSS normalized, so screenshots reflect final UI). -->

- README.md exists (rewritten 2026-06-14 per git log) — verify/refresh for launch, do not rewrite wholesale (D-12).
- LICENSE exists (MIT, verified). package.json license: MIT. D-12 = verify, not recreate.
- PERMISSIONS.md is created in plan 03 — reference it from PRIVACY.md/README, do not duplicate it.
- NFR-04 evidence (verified this session): no external `fetch` to remote hosts anywhere in src/; all I/O is chrome.storage / IndexedDB / local ONNX model.
- D-02: gate of record = manual Chrome Task Manager (Shift+Esc); the programmatic probe (logMemoryProbe in plan 01) is dev visibility only, NOT the gate.
- D-13: manual per-OS screenshots; automated visual-regression explicitly REJECTED (CONTEXT Deferred — CI is single-OS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRIVACY.md, MEMORY-RUNBOOK.md, CROSS-OS-SCREENSHOTS.md, STORE-LISTING.md + README/LICENSE polish</name>
  <files>PRIVACY.md, docs/MEMORY-RUNBOOK.md, docs/CROSS-OS-SCREENSHOTS.md, docs/STORE-LISTING.md, README.md</files>
  <read_first>
    - README.md (full — refresh, don't rewrite; D-12)
    - LICENSE (verify MIT present)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (New Files → Pattern Sources: PRIVACY/MEMORY-RUNBOOK/CROSS-OS-SCREENSHOTS)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (D-02 procedure, Pitfall 2 probe caveat, Validation Architecture)
    - .planning/phases/05-polishing-launch/05-01-PLAN.md (the idle-teardown mechanism the runbook measures)
  </read_first>
  <action>
    Create the launch docs:
    (1) `PRIVACY.md` (repo root, D-10): zero-telemetry statement (NFR-04) — Smart Hibernator sends no data off-device; all storage is local (chrome.storage.local + IndexedDB) and inference is a local ONNX model; document the verified evidence that there is no external `fetch` in `src/`. Link to PERMISSIONS.md (created in plan 03). Match README tone.
    (2) `docs/MEMORY-RUNBOOK.md` (D-02 gate of record): a reproducible procedure to (a) load the unpacked/zipped build, (b) open Chrome Task Manager (Shift+Esc), (c) open a many-tabs load (state an explicit count, e.g. 50 tabs), (d) read the extension's Service Worker + Offscreen Document + any open pages RSS, (e) wait past the OFFSCREEN_IDLE_MS window (~10 min) with no classification activity, (f) re-read and confirm the offscreen-document process is gone/reclaimed and total extension RAM is < 45MB. State the explicit pass threshold: "extension total RSS < 45MB". Note the dev probe (logMemoryProbe) is supplementary visibility only and is usually unavailable because the extension is not cross-origin-isolated (Pitfall 2) — do NOT add COOP+COEP to enable it.
    (3) `docs/CROSS-OS-SCREENSHOTS.md` (D-13 verify half): a per-OS checklist (Windows, macOS, Linux) listing each surface to screenshot (popup, dashboard Stats tab, dashboard Settings tab) and what to confirm (scrollbars thin/consistent, fonts render with the Geist→system fallback, no native-control divergence). Note this is a maintainer task — CI is single-OS.
    (4) `docs/STORE-LISTING.md` (D-11): CWS marketing description, feature bullets (smart AI hibernation, RAM savings dashboard, perfect state restoration, privacy-first/zero-telemetry), and a screenshot/promo-tile plan referencing the screenshots from CROSS-OS-SCREENSHOTS.md.
    (5) `README.md` (D-12): verify/refresh — ensure it reflects shipped AI + state restoration (git log says it was rewritten), link PRIVACY.md and PERMISSIONS.md, confirm LICENSE (MIT) is referenced. Do NOT rewrite wholesale; targeted refresh only.
  </action>
  <verify>
    <automated>test -f PRIVACY.md && test -f docs/MEMORY-RUNBOOK.md && test -f docs/CROSS-OS-SCREENSHOTS.md && test -f docs/STORE-LISTING.md && grep -qi "telemetry" PRIVACY.md && grep -qi "task manager" docs/MEMORY-RUNBOOK.md && grep -q "45" docs/MEMORY-RUNBOOK.md && grep -qi "macos" docs/CROSS-OS-SCREENSHOTS.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - All four new docs exist (PRIVACY.md, docs/MEMORY-RUNBOOK.md, docs/CROSS-OS-SCREENSHOTS.md, docs/STORE-LISTING.md)
    - PRIVACY.md states zero-telemetry and references the no-external-fetch evidence (source assertion)
    - MEMORY-RUNBOOK.md contains the Chrome Task Manager procedure with an explicit `< 45MB` pass threshold and a before/after idle-teardown read (source assertion)
    - CROSS-OS-SCREENSHOTS.md lists Windows + macOS + Linux and the surfaces to capture (source assertion)
    - STORE-LISTING.md has a description + feature bullets (source assertion)
    - README.md references PRIVACY.md and PERMISSIONS.md and LICENSE (source assertion)
  </acceptance_criteria>
  <done>All launch docs exist and are accurate; README refreshed; runbook states the ≤45MB gate procedure and threshold.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: NFR-01 memory gate of record (manual Chrome Task Manager reading)</name>
  <what-built>Plan 01 added offscreen idle-teardown + release; this plan delivered docs/MEMORY-RUNBOOK.md. The packaging script (plan 03) produces a loadable build.</what-built>
  <read_first>
    - docs/MEMORY-RUNBOOK.md (the procedure to follow)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (D-02, Pitfall 1, Pitfall 2)
  </read_first>
  <how-to-verify>
    1. Run `npm run package` (or `npm run build`) and load the built extension unpacked in Chrome 120+.
    2. Open ~50 real tabs across multiple domains; let the extension classify (alarm fires every 1 minute).
    3. Open Chrome Task Manager (Shift+Esc). Record the RSS of the Smart Hibernator Service Worker + Offscreen Document + any open pages. This is the "warm" reading.
    4. Stop interacting; wait past the OFFSCREEN_IDLE_MS idle window (~10 min) with no classification activity.
    5. Re-open Task Manager and confirm the Offscreen Document process is gone/reclaimed and total extension RAM is below 45MB. This is the "idle" reading — the NFR-01 gate of record.
    Expected: total extension RSS < 45MB after the idle window; offscreen document torn down then recreated transparently on the next classification burst with no errors.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms total extension RSS < 45MB after the idle window under the ~50-tab load (NFR-01 pass/fail gate)
    - Offscreen document observed to disappear after idle and reappear on next classification without console errors (D-01/D-05 behavior)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded warm/idle RAM numbers, or describe the failure (e.g. RAM still > 45MB, offscreen never torn down, cold-start thrash).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-OS UI screenshot pass (manual, per-OS)</name>
  <what-built>Plan 04 normalized scrollbars + fonts in popup/dashboard CSS; this plan delivered docs/CROSS-OS-SCREENSHOTS.md as the checklist.</what-built>
  <read_first>
    - docs/CROSS-OS-SCREENSHOTS.md (the per-OS checklist)
    - .planning/phases/05-polishing-launch/05-04-PLAN.md (what was normalized)
  </read_first>
  <how-to-verify>
    On each of Windows, macOS, and Linux (whoever has access to each OS):
    1. Load the built extension.
    2. Capture screenshots of the popup, dashboard Stats tab, and dashboard Settings tab per the checklist.
    3. Confirm: scrollbars are thin/consistent (no Windows fat native bars), fonts render via the Geist→system-ui fallback consistently, no native-control divergence.
    These screenshots double as the D-11 store-listing assets.
  </how-to-verify>
  <acceptance_criteria>
    - Screenshots captured on all three OSes (Windows, macOS, Linux) for popup + dashboard surfaces
    - Human confirms UI is visually consistent across the three OSes (scrollbars, fonts, controls) — success criterion 2
  </acceptance_criteria>
  <resume-signal>Type "approved" with the captured screenshots (or note which OS is pending), or describe per-OS inconsistencies found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension → user trust / CWS review | privacy claims and permission justifications are trust assertions verified at submission |
| documented memory claim → real runtime RAM | the ≤45MB claim must be empirically confirmed, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Repudiation / Information Disclosure | unverified privacy claim (claiming zero-telemetry without evidence) damages trust / risks CWS rejection | mitigate | PRIVACY.md documents the verified no-external-fetch evidence; claim is backed by source inspection (NFR-04). |
| T-05-14 | Denial of Service (resource) | shipping a build that exceeds the ≤45MB budget (NFR-01 unverified) | mitigate | D-02 manual Task Manager gate of record (Task 2) empirically confirms < 45MB before launch; blocks the phase gate if it fails. |

> The two manual checkpoints (Tasks 2 and 3) are inherently human gates: per RESEARCH/CONTEXT the NFR-01 measurement is a Task Manager reading and cross-OS UI verification cannot run on single-OS CI. No automation can substitute (automated visual-regression explicitly rejected in CONTEXT Deferred).
</threat_model>

<verification>
- Doc existence + content grep gates pass (Task 1).
- Task 2 (manual): human-confirmed total extension RAM < 45MB after idle teardown under ~50-tab load — the NFR-01 gate of record.
- Task 3 (manual): per-OS screenshots captured and UI confirmed consistent across Windows/macOS/Linux — success criterion 2 gate.
</verification>

<success_criteria>
- Launch docs complete: PRIVACY.md, MEMORY-RUNBOOK.md, CROSS-OS-SCREENSHOTS.md, STORE-LISTING.md, refreshed README (+ LICENSE verified).
- NFR-01 ≤45MB confirmed by the manual Task Manager gate of record.
- Cross-OS UI consistency confirmed by the per-OS screenshot pass.
- CWS launch package (docs + permissions + zip + listing copy + screenshots) is complete and ready for submission.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polishing-launch/05-05-SUMMARY.md`
</output>
