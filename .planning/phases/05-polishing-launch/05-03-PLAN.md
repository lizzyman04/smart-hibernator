---
phase: 05-polishing-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - package.json
  - PERMISSIONS.md
  - scripts/package.mjs
  - src/background/manifest.test.ts
autonomous: true
requirements: [NFR-05, NFR-06]
must_haves:
  truths:
    - "manifest.json no longer requests the scripting permission"
    - "The six kept permissions each have a one-line in-repo justification mapping to a real call site"
    - "A packaging command produces a CWS-ready zip of a freshly rebuilt dist/ with manifest at the zip root"
    - "Manifest metadata (name, description, version, homepage_url) and icons at 16/32/48/128 are present"
  artifacts:
    - path: "manifest.json"
      provides: "permissions array without scripting; metadata polish + homepage_url"
      contains: "homepage_url"
    - path: "PERMISSIONS.md"
      provides: "Per-permission justification table mapping each of the six to its call site"
      contains: "captureVisibleTab"
    - path: "scripts/package.mjs"
      provides: "build-then-zip packaging producing dist .zip with manifest at root"
      contains: "build"
    - path: "package.json"
      provides: "package npm script invoking the build-then-zip flow"
      contains: "package"
  key_links:
    - from: "scripts/package.mjs"
      to: "dist/ fresh build"
      via: "runs npm run build BEFORE zipping (never zips stale dist)"
      pattern: "build"
    - from: "PERMISSIONS.md"
      to: "manifest.json permissions"
      via: "one justification row per kept permission"
      pattern: "activeTab|contextMenus|offscreen"
---

<objective>
Trim and justify permissions for Chrome Web Store review (D-03 remove unused `scripting`; D-04 justify-each for the remaining six), polish manifest launch metadata (D-09), and add a packaging script that produces a CWS-ready zip from a freshly rebuilt `dist/` (D-09). All static/config work — no runtime behavior change. Removing `scripting` is a pure manifest deletion (grep-confirmed zero `chrome.scripting` usages in `src/`) with zero runtime data impact.

Purpose: Pass CWS least-privilege review (unused powerful permissions are a known review flag) and ship a reproducible, correct upload artifact that matches source (the stale `dist/` still contains `scripting` and must not be uploaded).
Output: Edited `manifest.json`, new `PERMISSIONS.md`, a `scripts/package.mjs` build-then-zip script + `package` npm script, and a manifest-assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polishing-launch/05-CONTEXT.md
@.planning/phases/05-polishing-launch/05-RESEARCH.md
@.planning/phases/05-polishing-launch/05-PATTERNS.md

<interfaces>
<!-- Current manifest (verified this session) and the call sites justifying each kept permission. -->

manifest.json current permissions (lines 35-43): ["storage","tabs","alarms","contextMenus","scripting","activeTab","offscreen"]
  → D-03 removes "scripting" only. Keep the other six.
manifest.json metadata: name "Smart Hibernator" (line 3), description (line 4), version "1.0.0" (line 5), icons declared 16/32/48/128 (lines 8-13 + 44-49, all four PNGs verified present in icons/). No homepage_url yet.

Permission → call-site map (verified — for PERMISSIONS.md / D-04):
- storage    → chrome.storage.local (settings, classification cache, counts) — throughout SW
- tabs       → chrome.tabs.query / discard / get / update (hibernation engine)
- alarms     → chrome.alarms hibernation tick (alarms.ts, periodInMinutes: 1)
- contextMenus → chrome.contextMenus right-click "Hibernate" (contextMenus.ts + index.ts line 221)
- activeTab  → chrome.tabs.captureVisibleTab() thumbnail capture (thumbnail.ts)
- offscreen  → ONNX Runtime Web in offscreen document (offscreen/main.ts)

package.json scripts (lines 5-10): dev, build (vite build), test, test:e2e. homepage already "https://github.com/lizzyman04/smart-hibernator#readme"; repository git URL present; license MIT.

Do NOT add web-ext (STATE.md: web-ext rejects chrome:// patterns). Use plain zip CLI or node adm-zip/archiver fallback.
Do NOT chase the NFR-05 four-permission ideal by removing activeTab/contextMenus — they back shipped features (D-04 "justify, don't amputate").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove scripting, polish manifest metadata, add manifest-assertion test (D-03/D-09)</name>
  <files>manifest.json, src/background/manifest.test.ts</files>
  <read_first>
    - manifest.json (full — permissions lines 35-43, metadata lines 1-5, icons lines 8-13/44-49)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (manifest.json section)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (D-03, Runtime State Inventory — scripting removal has zero stored-state impact)
  </read_first>
  <action>
    In `manifest.json`, delete the `"scripting"` entry from the `permissions` array (D-03 — grep-confirmed zero `chrome.scripting` usages in `src/` this session; no code change needed). Keep `storage`, `tabs`, `alarms`, `contextMenus`, `activeTab`, `offscreen`. Add `"homepage_url": "https://github.com/lizzyman04/smart-hibernator"` (D-09 metadata). Bump `"version"` from `1.0.0` to `1.0.1` (first launch-hardening release). Verify `name` and `description` are launch-appropriate (current values are acceptable — leave unless clearly deficient). Do NOT touch the commands block, content_scripts, web_accessible_resources, or CSP. Add `src/background/manifest.test.ts` that imports/reads `manifest.json` and asserts: `permissions` does NOT include `'scripting'`; `permissions` DOES include all six kept entries; `homepage_url` is present; `icons` has keys 16/32/48/128; `version` matches the bumped value. Use a static JSON import or fs read in the vitest test.
  </action>
  <verify>
    <automated>npx vitest run src/background/manifest.test.ts && node -e "const m=require('./manifest.json'); if(m.permissions.includes('scripting')) process.exit(1); if(!m.homepage_url) process.exit(1); console.log('MANIFEST_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"scripting"' manifest.json` returns 0
    - manifest.json contains `homepage_url` and version is `1.0.1`
    - `permissions` array contains exactly the six: storage, tabs, alarms, contextMenus, activeTab, offscreen (source assertion)
    - manifest.test.ts asserts scripting-absent + six-present + homepage_url + four icon sizes + version (behavior assertion)
    - `npx vitest run src/background/manifest.test.ts` exits 0
  </acceptance_criteria>
  <done>scripting removed; metadata polished; a test pins the permission set and metadata so regressions are caught.</done>
</task>

<task type="auto">
  <name>Task 2: PERMISSIONS.md justification doc (D-04/D-10)</name>
  <files>PERMISSIONS.md</files>
  <read_first>
    - manifest.json (the kept permissions after Task 1)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (PERMISSIONS.md section — call-site map)
    - .planning/phases/05-polishing-launch/05-CONTEXT.md (D-04 — justify, don't amputate)
    - README.md (repo-root doc tone/format to match)
  </read_first>
  <action>
    Create `PERMISSIONS.md` at the repo root: a short intro stating Smart Hibernator follows least-privilege (NFR-05) and removed the unused `scripting` permission, then a table with one row per KEPT permission mapping it to its concrete justification + call site: `storage` → settings/classification-cache/counts via chrome.storage.local; `tabs` → hibernation engine (chrome.tabs.query/discard/get/update); `alarms` → 1-minute hibernation tick (alarms.ts); `contextMenus` → right-click "Hibernate this tab" (contextMenus.ts); `activeTab` → thumbnail capture via chrome.tabs.captureVisibleTab() (thumbnail.ts); `offscreen` → local ONNX inference in the offscreen document (offscreen/main.ts). Explicitly note that `activeTab` and `contextMenus` are kept (not removed) because they back shipped functionality (D-04). This doc feeds the CWS submission permission-justification fields. Keep it concise and accurate to the verified call sites.
  </action>
  <verify>
    <automated>test -f PERMISSIONS.md && grep -q "captureVisibleTab" PERMISSIONS.md && grep -q "offscreen" PERMISSIONS.md && grep -q "scripting" PERMISSIONS.md && echo PERMISSIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - PERMISSIONS.md exists at repo root with one row per kept permission (six rows)
    - Each of storage, tabs, alarms, contextMenus, activeTab, offscreen appears with a call-site justification
    - `captureVisibleTab` (activeTab justification) and the scripting-removal note both present (source assertion)
    - No permission is listed that is not in manifest.json (consistency assertion)
  </acceptance_criteria>
  <done>Every kept permission has an accurate one-line justification mapped to a real call site; scripting removal noted.</done>
</task>

<task type="auto">
  <name>Task 3: Build-then-zip packaging script (D-09)</name>
  <files>scripts/package.mjs, package.json</files>
  <read_first>
    - package.json (scripts block lines 5-10)
    - vite.config.ts (build config — confirms dist/ output)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (Standard Stack supporting tools; Pitfall 6 — never zip stale dist)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (packaging section)
  </read_first>
  <action>
    Create `scripts/package.mjs` (node ESM, matches `"type": "module"`): it MUST run the production build FIRST (invoke `vite build` / `npm run build` via child_process) so `dist/manifest.json` reflects the removed `scripting` + bumped version (Pitfall 6 — never zip the stale dist), then produce a zip of the `dist/` directory contents with `manifest.json` at the ZIP ROOT (CWS requires manifest at top level of the archive). Prefer the system `zip` CLI; if unavailable, fall back to a node zip lib (adm-zip or archiver) — add the chosen lib to devDependencies only if the CLI fallback is needed. Name the output e.g. `smart-hibernator-<version>.zip` reading version from manifest.json. After zipping, log a verification line confirming `dist/manifest.json` contains no `scripting` and the expected version (fail/exit nonzero if it still contains scripting — guards Pitfall 6). Add a `"package": "node scripts/package.mjs"` entry to package.json scripts. Do NOT add web-ext (STATE: rejects chrome:// patterns).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.package) process.exit(1); console.log('PKG_SCRIPT_OK')" && test -f scripts/package.mjs && grep -q "build" scripts/package.mjs && echo PACKAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/package.mjs` exists and invokes a build step BEFORE any zip step (source assertion: build call precedes zip call)
    - package.json scripts contains `"package"` mapping to `node scripts/package.mjs`
    - The script asserts `dist/manifest.json` has no `scripting` before/after zipping and exits nonzero if it does (Pitfall 6 guard, source assertion)
    - The zip places manifest.json at the archive root (source assertion: zips dist contents, not the dist folder as a nested dir)
    - No `web-ext` dependency added (source assertion)
    - `npm run package` (manual smoke — runs build then produces the zip) is documented as the D-09 smoke check; automated gate is the grep/structure assertions above (full run requires a clean build environment)
  </acceptance_criteria>
  <done>npm run package rebuilds dist fresh and zips it with manifest at root, refusing to package a dist that still contains scripting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension → Chrome (declared permissions) | each permission is an authority grant the extension can exercise on the user's browser |
| source → uploaded CWS artifact (dist zip) | the uploaded zip must match source; a stale dist would ship removed permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Elevation of Privilege | over-broad permissions (unused `scripting`) enable abuse / fail CWS least-privilege review | mitigate | D-03 remove `scripting` (grep-confirmed unused); D-04 justify the remaining six in PERMISSIONS.md; manifest.test.ts pins the set against regression. |
| T-05-10 | Tampering | shipping a stale dist that still requests removed `scripting` / wrong version (Pitfall 6) | mitigate | package.mjs runs `npm run build` before zip and exits nonzero if `dist/manifest.json` still contains `scripting`. |
| T-05-11 | Repudiation | unjustified permissions causing CWS rejection / user distrust | mitigate | PERMISSIONS.md documents each kept permission's purpose + call site, feeding the CWS submission and the open-source trust pillar. |
</threat_model>

<verification>
- `npx vitest run src/background/manifest.test.ts` green; manifest grep gates pass.
- PERMISSIONS.md present with six justified rows + scripting-removal note.
- scripts/package.mjs structure assertions pass; `npm run package` smoke (build-then-zip) produces a zip with no scripting in dist/manifest.json.
</verification>

<success_criteria>
- scripting removed; six permissions justified in PERMISSIONS.md (NFR-05 / minimum-permissions success criterion).
- Manifest metadata polished (homepage_url, version bump) and icons verified at all four sizes.
- A reproducible packaging command produces a CWS-ready zip from a fresh build and refuses to ship a dist still containing scripting.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polishing-launch/05-03-SUMMARY.md`
</output>
