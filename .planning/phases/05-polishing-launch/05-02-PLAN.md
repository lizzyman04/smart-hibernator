---
phase: 05-polishing-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/restricted-urls.ts
  - src/shared/restricted-urls.test.ts
  - src/content/form-watcher.ts
  - src/content/form-watcher.test.ts
  - src/background/idb.ts
  - src/background/idb.test.ts
  - src/background/index.ts
  - src/background/index.test.ts
  - src/background/badge.ts
autonomous: true
requirements: [NFR-01]
must_haves:
  truths:
    - "Content script and discard path no-op cleanly on chrome://, chrome-extension://, Chrome Web Store, and other injection-blocked URLs — no console errors"
    - "IndexedDB QuotaExceededError triggers oldest-first eviction + one retry and never produces an unhandled rejection"
    - "Rapid tab open/close churn does not double-count hibernated_count or corrupt the badge"
    - "Browser startup with pre-discarded tabs does not re-count already-hibernated tabs"
  artifacts:
    - path: "src/shared/restricted-urls.ts"
      provides: "isInjectable(url) predicate + RESTRICTED_PREFIXES denylist"
      contains: "isInjectable"
    - path: "src/content/form-watcher.ts"
      provides: "Top-of-file restricted-URL early-return guard (inlined denylist, no import)"
      contains: "chrome-extension://"
    - path: "src/background/idb.ts"
      provides: "Quota-exceeded guard wrapping write paths, reusing pruneIfNeeded eviction"
      contains: "QuotaExceededError"
  key_links:
    - from: "src/content/form-watcher.ts top of file"
      to: "early return before any listener/sendMessage"
      via: "inlined restricted-URL check on location.href"
      pattern: "chrome-extension://|chrome://"
    - from: "src/background/idb.ts write paths"
      to: "pruneIfNeeded eviction"
      via: "catch QuotaExceededError → evict → retry once"
      pattern: "QuotaExceeded"
---

<objective>
Harden the three remaining edge-case failure classes over the feature-frozen extension: restricted-page no-op (D-06), IndexedDB quota-exceeded graceful handling (D-07), and rapid-churn / startup-restore / cold-start integrity (D-07/D-08). This is assert-and-extend work over established chokepoints — most correct patterns already exist (single atomic storage read, top-level listeners, non-undefined discard counting, oldest-first eviction); this plan adds the missing guards and the tests that pin the invariants.

Purpose: Make the shipped extension survive the failure modes a CWS launch and 1M-user scale will surface, without changing any user-facing behavior (functionality is frozen).
Output: New `src/shared/restricted-urls.ts` helper + truth-table test, a top-of-file guard inlined into the content script, a quota guard in `idb.ts`, and extended `index.test.ts` churn/startup/cold-start coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polishing-launch/05-CONTEXT.md
@.planning/phases/05-polishing-launch/05-RESEARCH.md
@.planning/phases/05-polishing-launch/05-PATTERNS.md

<interfaces>
<!-- Established chokepoints to extend. CRITICAL invariants from STATE.md: ALL IDB writes stay in SW only; single atomic storage.local.get per tick; content scripts are IMPORT-FREE (inline constants); listeners registered synchronously at module top level; Chrome 120 messaging (literal return true). -->

From src/background/hibernation.ts (existing — D-06 SW side already satisfied):
- isDiscardable() guards `!tab.url.startsWith('http://') && !tab.url.startsWith('https://')` (line 17)
- discard counting only increments hibernated_count on non-undefined chrome.tabs.discard() return (lines 122-123) — naturally idempotent under double-discard

From src/background/idb.ts (existing — wrap write paths, reuse eviction):
- `pruneIfNeeded()` (lines 98-112) — oldest-first eviction by capturedAt, REUSE this; do not build new eviction
- write paths: putThumbnail (~74-77), appendTabHistory (~119-122), putDomainBias (~171-174), putTabState (~181-184) — all `const db = await getDb(); await db.put(...)`
- `dbPromise` singleton + `blocking()` close handler — preserve

From src/background/index.ts (existing — assert + tighten):
- All listeners registered synchronously at top level (onActivated/onRemoved/onUpdated/onMessage/onInstalled/onStartup)
- lastActiveTabId reset on removal (~lines 146-148)
- single atomic storage.local.get([...]) per alarm tick; every read uses `?? default` fallback
- tabId from sender.tab?.id only; literal `return true` for async sendResponse (line 185)
- onUpdated `discarded === false` check ordered BEFORE `status === 'complete'` early-return (lines 104, 125) — preserve ordering (CR-02)

From src/background/badge.ts (existing — verify-only):
- `updateBadge(count)` is a pure function of count (lines 1-7); badge derives purely from persisted hibernated_count; chrome.action is manually mocked in vitest.setup.ts

Content-script no-import convention: form-watcher.ts inlines constants (DEBOUNCE_MS, MAX_FIELDS) identical to shared/constants.ts. The restricted-URL list MUST be inlined here too — do NOT add an import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: restricted-urls helper + content-script top-of-file guard (D-06)</name>
  <files>src/shared/restricted-urls.ts, src/shared/restricted-urls.test.ts, src/content/form-watcher.ts, src/content/form-watcher.test.ts</files>
  <read_first>
    - src/content/form-watcher.ts (top of file lines 1-57 — no-import convention; the guard goes ABOVE reportFormActivity and all listener registration)
    - src/content/form-watcher.test.ts (full — extend with restricted-URL cases; uses jsdom location)
    - src/background/hibernation.ts (line 17 — the startsWith('http') convention the helper mirrors)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (Pattern 3)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (restricted-urls section + form-watcher section)
  </read_first>
  <behavior>
    - isInjectable(undefined) === false; isInjectable('') === false
    - isInjectable('chrome://settings') === false; isInjectable('chrome-extension://abc/x') === false; isInjectable('edge://flags') === false; isInjectable('about:blank') === false; isInjectable('devtools://x') === false; isInjectable('view-source:http://x') === false; isInjectable('chrome-untrusted://x') === false
    - isInjectable('https://chromewebstore.google.com/...') === false; isInjectable('https://chrome.google.com/webstore/...') === false
    - isInjectable('https://github.com') === true; isInjectable('http://localhost:3000') === true
    - In the content script, when location URL is restricted, reportFormActivity is never called and no DOM/input listeners are registered (early return at module top)
  </behavior>
  <action>
    Create `src/shared/restricted-urls.ts` exporting `const RESTRICTED_PREFIXES` (chrome://, chrome-extension://, edge://, about:, devtools://, view-source:, chrome-untrusted://, https://chromewebstore.google.com, https://chrome.google.com/webstore) and `export function isInjectable(url: string | undefined): boolean` that returns false for falsy url, false unless url starts with http:// or https://, and false if any RESTRICTED_PREFIXES entry is a prefix — else true (mirrors hibernation.ts startsWith convention; RESEARCH Pattern 3). Add `src/shared/restricted-urls.test.ts` with the full truth table from `<behavior>`. In `src/content/form-watcher.ts`, INLINE the same denylist + check at the VERY TOP of the file (do NOT import — content scripts are import-free per the verified Phase 4 convention; duplicate the prefixes exactly as the file already duplicates DEBOUNCE_MS/MAX_FIELDS). The guard reads the page URL (e.g. location.href / window.location.href) and, if not injectable, returns early so reportFormActivity and all event listeners never register — no console errors on chrome:// (D-06 / Information-Disclosure mitigation). Extend form-watcher.test.ts to assert the early-return path. Note: SW discard path is already http-guarded in hibernation.ts isDiscardable, so D-06's only code gap is this content-script guard plus the CWS store host prefixes (now in the shared helper for any SW-side use).
  </action>
  <verify>
    <automated>npx vitest run src/shared/restricted-urls.test.ts src/content/form-watcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/restricted-urls.ts` exports `isInjectable` and `RESTRICTED_PREFIXES`; truth-table test exits 0
    - form-watcher.ts contains an inlined restricted check referencing `chrome-extension://` and `chrome://` at the top of the file BEFORE any addEventListener / sendMessage (source assertion)
    - `grep -c "^import" src/content/form-watcher.ts` returns 0 (still import-free)
    - form-watcher.test.ts asserts no listener registration on a restricted URL (behavior assertion)
    - `npx vitest run src/shared/restricted-urls.test.ts src/content/form-watcher.test.ts` exits 0
  </acceptance_criteria>
  <done>isInjectable truth table passes; content script no-ops on restricted pages without imports or console errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IndexedDB quota-exceeded guard reusing eviction (D-07)</name>
  <files>src/background/idb.ts, src/background/idb.test.ts</files>
  <read_first>
    - src/background/idb.ts (full — pruneIfNeeded lines 98-112; the four write paths; dbPromise singleton; blocking() handler)
    - src/background/idb.test.ts (full — uses fake-indexeddb; extend with a forced-quota case)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (Pattern 4, Pitfall — Don't Hand-Roll eviction)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (idb.ts section)
  </read_first>
  <behavior>
    - When a write throws DOMException with name 'QuotaExceededError' (or 'QuotaExceeded'), the guard calls the eviction fn (pruneIfNeeded) then retries the write once
    - If the retry also throws, the guard swallows it silently — the returned promise resolves (no unhandled rejection)
    - Non-quota errors fall through to the existing behavior (not newly swallowed by this guard beyond current call-site handling)
  </behavior>
  <action>
    Add a `putWithQuotaGuard(write: () => Promise<void>, evict: () => Promise<void>)` helper in `idb.ts` per RESEARCH Pattern 4: `try { await write() } catch (e) { if (e instanceof DOMException && (e.name === 'QuotaExceededError' || e.name === 'QuotaExceeded')) { await evict().catch(() => {}); try { await write() } catch {} } else throw e }`. Wrap the thumbnail/state write paths that can grow unbounded (putThumbnail and putTabState are the size-bearing stores; appendTabHistory/putDomainBias are small but may also be wrapped with their respective evictors if a natural one exists — for thumbnails the evictor is the existing `pruneIfNeeded`). Reuse `pruneIfNeeded` as the evict callback for thumbnails (oldest-first by capturedAt — do NOT build new eviction logic, Don't Hand-Roll). Preserve the `dbPromise` singleton and `blocking()` close handler. Keep ALL IDB writes in the SW (Phase 3 invariant — do not move IDB into offscreen/content). Extend idb.test.ts: mock/force a QuotaExceededError on the first write, assert the evictor is called and the second write succeeds, and assert that when both writes fail the returned promise still resolves (no rejection escapes).
  </action>
  <verify>
    <automated>npx vitest run src/background/idb.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QuotaExceeded" src/background/idb.ts` returns ≥ 1
    - `putWithQuotaGuard` (or equivalently named guard) wraps at least putThumbnail and putTabState (source assertion)
    - The evict callback for thumbnails is the existing `pruneIfNeeded` — no new eviction loop added (source assertion)
    - Test: forced QuotaExceededError → eviction called → retry succeeds (behavior assertion)
    - Test: quota error on both attempts → returned promise resolves, no unhandled rejection (behavior assertion)
    - `npx vitest run src/background/idb.test.ts` exits 0
  </acceptance_criteria>
  <done>Quota-exceeded writes evict-and-retry then give up silently; no unhandled rejection; eviction reuses pruneIfNeeded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Churn / startup-restore / cold-start integrity assertions (D-07/D-08)</name>
  <files>src/background/index.ts, src/background/index.test.ts, src/background/badge.ts</files>
  <read_first>
    - src/background/index.ts (full — listeners, lastActiveTabId reset lines 146-148, single atomic storage read, onUpdated ordering lines 104/125, sender.tab.id usage)
    - src/background/index.test.ts (full — uses callListeners() for onActivated/onRemoved/onUpdated; extend with churn + startup + cold-start cases)
    - src/background/badge.ts (full — pure updateBadge(count); chrome.action manual mock)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (Pitfall 4, Pitfall 5)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (index.ts section + badge.ts section)
  </read_first>
  <behavior>
    - Rapid interleaved onActivated/onRemoved (callListeners burst) does not increment hibernated_count for tabs that were not actually discarded; lastActiveTabId is reset on removal
    - badge text derives purely from the persisted hibernated_count (updateBadge is a pure function: 0 → '', N → String(N), ≥1000 → '999+')
    - Cold-start: a storage read on a fresh/empty storage (no onInstalled defaults) does not throw — every consumer uses `?? default` (assert handleAlarmTick / message handlers tolerate empty storage)
    - Browser startup with pre-discarded tabs (onStartup) does not re-increment hibernated_count for tabs already counted when first discarded
  </behavior>
  <action>
    This is assert-and-extend, NOT a rewrite (Pitfall 4/5). First VERIFY the invariants in code: (a) every listener is registered synchronously at module top level — no addListener inside an async callback (D-08 no listener leak); (b) lastActiveTabId reset in onRemoved; (c) onUpdated `discarded === false` ordered before `status === 'complete'`; (d) every storage read uses `?? default`. If any invariant is actually violated, fix it minimally; otherwise leave code unchanged and ADD tests pinning it. In index.test.ts add: a churn test using `callListeners()` to fire rapid onActivated/onRemoved interleavings and assert hibernated_count is not double-incremented and the badge matches the count; a cold-start test that drives handleAlarmTick (or the relevant handler) against an empty storage mock and asserts no throw / no `undefined is not iterable`; a startup-restore test (onStartup with pre-discarded tabs in chrome.tabs.query mock) asserting no re-count. For badge.ts, add/extend a pure-function test (0 → '', 5 → '5', 1500 → '999+') using the manual chrome.action mock (never `.mockReturnValue` on the real emitters — use the manual mock per STATE invariant). Preserve all Chrome 120 conventions (literal return true) when touching handlers. Do NOT change the single-atomic-storage-read-per-tick pattern.
  </action>
  <verify>
    <automated>npx vitest run src/background/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.test.ts contains a churn test using `callListeners()` asserting hibernated_count is not double-incremented under interleaved onActivated/onRemoved (behavior assertion)
    - index.test.ts contains a cold-start test against empty storage asserting no throw (behavior assertion)
    - index.test.ts contains a startup-restore test (onStartup + pre-discarded tabs) asserting no re-count (behavior assertion)
    - badge test asserts updateBadge(0) → '', updateBadge(1500) → '999+' (behavior assertion)
    - No new `addListener` was added inside any async callback (source assertion: listener registration count at top level unchanged)
    - `npx vitest run src/background/index.test.ts` exits 0
  </acceptance_criteria>
  <done>Churn, cold-start, and startup-restore invariants are test-pinned; badge integrity proven; no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web page (any URL) → content script | form-watcher runs on <all_urls>, including restricted pages where injection/messaging must no-op |
| content script / web context → SW (chrome.runtime messages) | best-effort messages; SW may be cold-starting |
| stored data growth → IndexedDB quota | unbounded thumbnail/state writes can hit the browser quota (local DoS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | content script erroring on chrome:// / extension pages leaks stack/info to console | mitigate | D-06 top-of-file isInjectable guard: early-return before any listener/messaging on restricted URLs; truth-table test. |
| T-05-06 | Denial of Service | unbounded IndexedDB growth exhausts quota (local DoS) | mitigate | D-07 putWithQuotaGuard: catch QuotaExceededError → pruneIfNeeded eviction → retry once → swallow; no unhandled rejection. Reuses existing oldest-first eviction + 14-day history prune. |
| T-05-07 | Tampering | badge/count corruption under rapid tab churn (drifting hibernated_count) | mitigate | D-08: discard counting already idempotent (only counts non-undefined discard returns); badge is a pure function of persisted count; lastActiveTabId reset on removal; churn + startup tests pin the invariants. |
| T-05-08 | Spoofing | message handler trusting message-body tabId instead of sender.tab.id | accept (already mitigated) | Phase 4 invariant: tabId taken from sender.tab?.id only; preserved when touching handlers. No new code path added. |
</threat_model>

<verification>
- `npx vitest run src/shared/restricted-urls.test.ts src/content/form-watcher.test.ts src/background/idb.test.ts src/background/index.test.ts` all green.
- All grep gates pass.
- Restricted-page no-op, quota eviction, and churn/startup integrity are each proven by an automated test.
</verification>

<success_criteria>
- Content script + discard path no-op cleanly on restricted URLs (D-06), proven by truth table + early-return test.
- IDB quota-exceeded evicts-and-retries with no unhandled rejection (D-07), proven by forced-quota test.
- Churn / startup-restore / cold-start do not corrupt hibernated_count or badge (D-07/D-08), proven by callListeners churn + startup + cold-start tests.
- All task test commands exit 0; no user-facing behavior changed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polishing-launch/05-02-SUMMARY.md`
</output>
