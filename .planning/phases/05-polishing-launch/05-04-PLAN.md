---
phase: 05-polishing-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/popup/index.css
  - src/dashboard/index.css
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "Scrollbars render as consistent thin bars across OSes (Windows fat native bars normalized)"
    - "The app font stack has explicit per-OS fallbacks after Geist (system-ui, -apple-system, Segoe UI, Roboto)"
    - "No raw native <select> or <input type=range> exists in popup/dashboard that would render per-OS (verified)"
  artifacts:
    - path: "src/popup/index.css"
      provides: "Tailwind @layer base scrollbar + font normalization"
      contains: "scrollbar-width"
    - path: "src/dashboard/index.css"
      provides: "Same @layer base normalization (sibling)"
      contains: "scrollbar-width"
  key_links:
    - from: "src/popup/index.css @layer base"
      to: "cross-OS scrollbar + font rendering"
      via: "scrollbar-width: thin + ::-webkit-scrollbar rules + font fallback chain"
      pattern: "::-webkit-scrollbar"
---

<objective>
Normalize the known per-OS rendering differences (D-13 fix half): custom thin scrollbars (Windows shows fat native bars vs macOS overlay), a `system-ui` font stack with explicit per-OS fallbacks after the project's Geist variable font, and verification that no raw native controls (`<select>`, `<input type=range>`) leak per-OS rendering. The shadcn slider/switch/scroll-area are Radix-based and already render consistently — the real risk is scrollbars and font metrics. This is the engineering half of cross-OS UI consistency (Phase 5 success criterion 2); the documented manual per-OS screenshot verification ships in plan 05.

Purpose: Make the popup and dashboard look consistent on Windows, macOS, and Linux so the CWS listing screenshots and real users see one design, not three.
Output: Extended `@layer base` blocks in `src/popup/index.css` and `src/dashboard/index.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polishing-launch/05-CONTEXT.md
@.planning/phases/05-polishing-launch/05-RESEARCH.md
@.planning/phases/05-polishing-launch/05-PATTERNS.md

<interfaces>
<!-- The existing @layer base block in popup/index.css (lines ~120-130) is the established normalization surface. Tailwind v4 @layer base. The dashboard CSS shares this structure (sibling). Add to these blocks — do NOT create a new global stylesheet. -->

src/popup/index.css existing @layer base:
  * { @apply border-border outline-ring/50; }
  body { @apply bg-background text-foreground; }
  html { @apply font-sans; }   /* font-sans theme = 'Geist Variable', sans-serif */

VERIFIED this session: zero `type="range"` and zero `<select>` in src/popup and src/dashboard, so control-normalization scope is empty — scrollbars + fonts are the only targets (RESEARCH A4 / Open Question 2).

Frozen-functionality boundary: this is styling-only. No layout/behavior changes — only cross-OS rendering normalization.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-OS scrollbar + font normalization in popup + dashboard CSS</name>
  <files>src/popup/index.css, src/dashboard/index.css</files>
  <read_first>
    - src/popup/index.css (full — the existing @layer base block to extend)
    - src/dashboard/index.css (full — sibling structure)
    - .planning/phases/05-polishing-launch/05-RESEARCH.md (Pattern 5 + Open Question 2 / A4)
    - .planning/phases/05-polishing-launch/05-PATTERNS.md (popup/dashboard index.css section)
  </read_first>
  <action>
    Re-confirm (grep) there is no `<select>` or `<input type="range">` in `src/popup` and `src/dashboard` before sizing scope; if any exist, normalize them too — otherwise control normalization is out of scope (Radix controls render consistently). Extend the EXISTING `@layer base` block in `src/popup/index.css` (do NOT add a new global stylesheet) with RESEARCH Pattern 5: a thin-scrollbar normalization — `* { scrollbar-width: thin; }`, `*::-webkit-scrollbar { width: 8px; height: 8px; }`, `*::-webkit-scrollbar-thumb { background: rgb(0 0 0 / 0.2); border-radius: 4px; }` (Windows fat native bars vs macOS overlay are the real per-OS risk) — and a font stack with explicit per-OS fallbacks after the project's Geist variable font: define `--app-font: "Geist Variable", system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;` and apply it to `html` with `-webkit-font-smoothing: antialiased` (so system-ui does not resolve to a different metric per OS). Preserve the existing `@apply` rules in the block. Apply the IDENTICAL normalization to `src/dashboard/index.css`'s `@layer base` (sibling exact match). Keep changes styling-only — no layout or behavior change (frozen functionality). Use color tokens consistent with the existing theme where a thumb color token exists; the rgb fallback above is acceptable if no token fits.
  </action>
  <verify>
    <automated>grep -q "scrollbar-width" src/popup/index.css && grep -q "::-webkit-scrollbar" src/popup/index.css && grep -q "scrollbar-width" src/dashboard/index.css && grep -q "system-ui\|--app-font" src/popup/index.css && npx vite build >/dev/null 2>&1 && echo CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both `src/popup/index.css` and `src/dashboard/index.css` contain `scrollbar-width: thin` and `::-webkit-scrollbar` rules inside `@layer base` (source assertion)
    - A font fallback chain including `system-ui` and at least `-apple-system` / `Segoe UI` is present (source assertion)
    - The pre-existing `@apply border-border` / `bg-background` rules are still present (no regression — source assertion)
    - `grep -rc 'type="range"\|<select' src/popup src/dashboard` returns 0 (confirms no native controls needing normalization) OR any found control is also normalized
    - `npx vite build` succeeds (CSS compiles under Tailwind v4)
  </acceptance_criteria>
  <done>Both stylesheets normalize scrollbars + fonts in @layer base; build passes; no native controls left un-normalized; styling-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS rendering engine → extension UI | each OS renders scrollbars/fonts/native controls differently, breaking visual consistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | (Reliability/UX — non-security) | per-OS scrollbar + font divergence in popup/dashboard | mitigate | D-13 CSS normalization in @layer base (thin scrollbars + explicit font fallbacks). This is a UI-consistency concern, not a security threat; no attacker boundary. Verified per-OS by the manual screenshot pass in plan 05. |

> Note: This is a styling-only plan over frozen functionality; it introduces no new attack surface, trust boundary, or untrusted-input path. The single entry is a UX-consistency reliability item, not a STRIDE security threat.
</threat_model>

<verification>
- `grep scrollbar-width / ::-webkit-scrollbar / system-ui` gates pass in both CSS files.
- `npx vite build` succeeds.
- Per-OS visual confirmation is delivered by `docs/CROSS-OS-SCREENSHOTS.md` + the manual screenshot checkpoint in plan 05 (manual gate — CI is single-OS).
</verification>

<success_criteria>
- Scrollbars and fonts normalized across OSes in both popup and dashboard (D-13 fix half).
- No native controls left rendering per-OS (verified).
- Build passes; functionality unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polishing-launch/05-04-SUMMARY.md`
</output>
